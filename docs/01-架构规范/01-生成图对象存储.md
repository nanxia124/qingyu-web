# 生成的图片存在哪

## 1. 这是什么

用户在我们网站上 AI 生图之后，生成的图片要长期保存，下次打开"我的作品"还能看到。这个文档讲图存在哪、怎么存最好。

## 2. 用户怎么用

- 用户在生图页写提示词，点"生成"
- 几秒钟后图出现在页面上
- 关掉浏览器，下次再打开，点"我的作品"，图还在
- 图可以下载到本地，可以分享给别人
- 图不会自己消失，除非用户手动删除

## 3. 数据是怎么流的

```
用户点生成
  → 后端调用上游 AI 模型
  → 上游返回一张图
  → 后端把这张图存成文件
  → 数据库记一行"第 N 张图，文件名叫 xxx"
  → 用户刷新页面，后端从文件里读出发给他看
```

## 4. 现在长什么样（当前实现）

打开"我的作品"页面，能看到过去生过的所有图，一行一行列出来。点开任意一张，大图展示在中间，旁边有"下载"和"分享"按钮。

**已经做了的**：
- ✅ 生图完成后图自动存下来，不依赖浏览器
- ✅ 关掉浏览器再打开，图还在
- ✅ 老用户以前生的图也能看到（自动兼容老数据）

**还没做的**：
- ❌ 图还是存在自己服务器硬盘上，没搬云存储
- ❌ 图加载速度一般，没接 CDN
- ❌ 图不能直接分享到微信/微博，只能下载后自己发

## 5. 存在哪最好

| 方案 | 优点 | 缺点 |
|---|---|---|
| 塞进数据库（老办法） | 代码简单 | 库胖 33%，列表越查越慢，备份越来越大 |
| 存在自己服务器硬盘 | 不花钱，现在就在这么做 | 磁盘会满，硬盘坏了图就没了 |
| 存在腾讯云对象存储 | 无限扩容、多副本不丢、不占自己磁盘 | 要花钱（每月几块钱） |
| 存在 CDN 上 | 用户看图最快 | 比对象存储贵 |

**规划**：先用自己硬盘（已做完），下一步搬腾讯云，以后接 CDN。

## 6. 现在做到哪了

| 阶段 | 状态 |
|---|---|
| 图不再塞数据库，改存硬盘 | ✅ 刚做完，待你实际生一张图验证 |
| 搬到腾讯云对象存储 | ⏳ 等你开通 COS |
| 老图从数据库里搬出来 | ⏳ 等搬云存储 |
| 接 CDN 加速 | ⏳ 以后再说 |

## 7. 出问题怎么办

后端有个开关 `GEN_OUTPUTS_TO_STORAGE=off`，加上重启就回到老办法（图塞数据库）。新方案出 bug 就这么回退。

## 8. 花多少钱

- 现在：0 元
- 搬腾讯云后：每月大概 10 块钱以内，新用户免费额度内基本不要钱

*AI 对照：abstraction scripts/deploy/object-store.mjs; layout OBJECT_DATA_DIR/gen/{taskId}/{index}.{ext}; tables app.file_objects (storage_provider,bucket,object_key,checksum,size_bytes,mime_type) + app.generation_outputs (task_id,file_id,output_type,metadata); worker runGenerationTaskWorker→persistGeneratedImage; read endpoint GET /api/generation-tasks/:id/outputs/:index/content?token=xxx; 前端 src/lib/generationTasks.ts taskOutputToDataUrls 优先走新接口，老 b64_json fallback; env GEN_OUTPUTS_TO_STORAGE=off rollback; P2 OBJECT_STORAGE_PROVIDER=s3 region ap-guangzhou; refs PostgreSQL wiki BinaryFilesInDB, volcengine veImageX。*