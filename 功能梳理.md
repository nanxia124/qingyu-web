# 轻域（QingYu / LitZone）功能全景图

> 基于源码梳理的轻域全站功能树，按"大组 → 小组 → 具体功能"分层。
> 标注说明：✅ 已接通后端可用；🟡 前端占位/模拟中；⬜ 未开始/入口预留。

---

## 一、创作工作台

### 1. 首页 / 工作台 `/`

- 快捷创作入口卡片
  - 文生图 → 跳转 `/image`
  - 扩图 → 跳转 `/image`
  - 风格化 → 跳转 `/image`
  - 我的作品 → 跳转 `/favorites`
- 快捷工具入口（占位）
  - WebUI（云端部署）
  - ComfyUI（节点式工作流）
  - 训练 LoRA（自定义模型）
- 灵感瀑布流（示例数据）
  - 分类筛选：全部 / 电商 / 海报 / 摄影 / 插画
  - 排序：推荐 / 最新
  - 点赞数展示
- 底部悬浮快捷输入框
  - 输入提示词 → 一键跳生图
  - 语音输入按钮
- 顶栏全局搜索（搜灵感/作品/教程）

### 2. AI 对话 `/chat` 🟡

> 当前为纯 UI 占位壳，未接后端。
> 无消息历史持久化（刷新即清空）、无多轮对话上下文、无流式输出、无 Markdown 渲染、无图片/文件展示。
> 发送后 400ms 模拟延迟，固定回复"服务待开通"。

- 聊天界面框架（当前回复"服务待开通"）
- 空状态欢迎页（大标题 + 副标题）
- 消息列表（用户右对齐气泡 / AI 左对齐无背景）
- 复制 AI 回复（复制按钮 + "已复制"状态 + 失败提示）
- AI 消息时间戳（每条回复下面显示"刚刚"）
- 附件按钮（占位，点击提示"即将上线"）
- 语音输入（追加到输入框末尾，不替换）
  - 三种状态：未开始（灰色麦克风）/ 加载中（转圈）/ 录音中（红色麦克风）
  - 支持实时部分识别（边说边出字）
  - 点击开始/停止
- 统一模型选择器（右上角，全局 ModelPicker 组件，与生图/视频页共用同一套模型目录）
  - 从服务端拉取可用模型列表
  - 按品牌分组（GPT / Grok / 豆包 Doubao / MiniMax / DALL-E / Veo 等）
  - 下拉选择，配置就绪后自动选默认模型
  - 带在线状态绿点
- 发送中 loading（"思考中..."脉冲动画）
- 输入框自动增高（最高 160px）
- 快捷键：Enter 发送，Shift+Enter 换行
- 发送按钮状态：有输入时高亮，空输入时灰色禁用

**布局细节：**
- 整个页面深色背景，flex 纵向布局，占满全屏高度
- 右上角统一模型选择器（全局 ModelPicker 组件，与生图/视频页共用同一套模型目录，按品牌分组下拉选择）
- 空状态：垂直水平居中，大标题 + 副标题
- 消息区最大宽度 820px 居中，消息间距 24px
- 用户消息：右对齐，灰色气泡，圆角 12px，最大宽度 520px，内边距 16px/12px
- AI 消息：左对齐，无背景，文字直接显示，最大宽度 640px
- AI 消息 meta 行：时间戳 + 复制按钮，间距 12px，文字 12px 灰色
- 复制按钮：28px 圆形，hover 变色
- 发送中状态：消息列表底部，左对齐，脉冲圆点
- 输入区最大宽度 600px 居中，卡片式容器，圆角 12px，hover 变深
- 输入框内按钮：34px 圆形，左右排列（附件在左，语音+发送在右），按钮间距 8px
- 输入框卡片最小高度 60px，内容底部对齐
- 附件按钮：Plus（+）图标 18px
- 发送按钮：Send（纸飞机）图标 16px，有输入时高亮色，空输入时灰色
- 消息文字：14px 字号，24px 行高
- 当前没有头像（用户消息和 AI 消息都没有头像）
- 当前用户消息没有时间戳（只有 AI 消息有）

**交互细节：**
- 复制成功后 1.2 秒内按钮变成"已复制"文字
- 发送中：脉冲圆点 + "思考中..."动画

**目前缺失的功能（待开发）：**
- 对话管理：新建对话 / 历史对话列表 / 删除对话 / 对话重命名
- 消息操作：重新生成 AI 回复 / 停止生成 / 编辑已发送消息 / 点赞点踩反馈
- 内容展示：Markdown 渲染 / 代码块高亮 / 流式输出 / 图片文件消息
- 输入增强：拖拽上传 / @提及引用 / 快捷指令模板 / 字数统计
- 体验：一键到底按钮 / 发送失败重试 / Token 用量 / 对话导出 / 对话分享
- 对话智能：标题自动生成 / 长对话自动摘要 / 下一步推荐
- 输入控制：预设角色人格 / 创造性温度调节 / 联网搜索开关
- 消息交互：引用回复指定消息 / 语音播报 AI 回复 / 按日期分组显示
- 管理：搜索历史对话 / 对话归档文件夹 / 一键清空所有对话
- 多模态：发图片看图说话 / 上传文档分析总结 / 发语音转文字处理
- 对话高级：对话分支分叉 / 消息撤回 / 草稿自动保存 / 中途换模型不丢上下文 / 单条消息失败单独重试
- 内容渲染：数学公式 LaTeX / 表格渲染 / 图表直接生成 / 思维链推理过程展示
- 其他：上下文轮数设置 / 消息精确时间戳 / 移动端手势支持
- 智能体与知识库：自定义 Bot 智能体 / 挂载个人知识库 RAG / 插件系统
- 协作效率：消息收藏 / 对话置顶 / 全局快捷键唤起 / 未读消息红点
- 高级交互：语音通话模式 / 多窗口多对话 / 自动化工作流触发
- 排版体验：字体大小调节 / 导出格式选择 / 对话内搜索关键词 / 分页加载更早历史
- 隐私安全：多设备同步对话 / 隐私模式不留记录 / 定时自动删除对话
- 引导提示：新用户空状态引导 / 错误友好提示 / 一键总结整个对话
- 模型与 Prompt：多模型对比回答 / Prompt 版本管理
- 输出能力：AI 生成 Word/Excel/PPT 文件下载 / 对话里直接生图 / 对话里直接生视频 / 多模态混合回复（文字+图片+表格）
- 消息操作：选中文字追问 / 选中文字总结翻译 / 消息翻译 / 消息书签标记
- 快捷效率：快捷回复短语 / 全局悬浮窗唤起 / 快捷键大全
- 设置偏好：回车行为自定义 / 自动滚动开关 / 发送后是否清空输入框
- 内容管理：对话内文件集中查看 / 对话内图片画廊 / 消息表情回应 / 未读分割线
- 导入导出：导出为长图 / 直接打印
- 其他实用：对话静音通知 / 消息个人便签备注 / 消息高亮标注
- 状态提示：发送失败红标可重发 / 断网提示自动重连 / 间隔久显示时间戳
- 上下文控制：一键清除记忆 / 上下文轮数预览 / 消息置顶到输入框
- 内容操作：表格导出 Excel / 图表下载图片 / 手动编辑 AI 回复
- 交互细节：消息右键菜单 / 选中文字直接复制 / 新消息桌面通知+提示音
- 语音输出：TTS 声音选择 / 朗读速度调节 / AI 回复自动朗读
- 对话管理：自动生成对话大纲目录 / 长时间未动自动归档
- 布局优化：左侧对话历史侧栏 / 用户和 AI 头像 / 用户消息时间戳 / 日期分割线 / 空状态推荐问题卡片 / 输入区工具栏增强
- 视觉层次：消息气泡阴影 / 气泡小三角尾巴 / 正在输入指示器 / 未读消息高亮
- 布局调节：左侧栏可折叠 / 专注模式 / 消息圆角大小可调
- 动画效果：消息渐入动画 / 发送按钮动效 / AI 打字机效果 / hover 显示操作按钮
- 交互细节：长消息自动折叠 / 消息选中高亮 / 强调色自定义 / 深色对比度优化
- 手势快捷键：双击复制 / 长按弹菜单 / 拖拽文件上传 / Esc 关弹窗 / 双击聚焦输入框
- 消息操作：消息转发 / 标记未读 / 批量选择删除 / 上滑显示精确时间戳
- 输入框增强：表情选择器 / 斜杠快捷指令 / 输入历史记录 / 代码片段插入
- 通知提醒：提示音自定义 / 桌面角标未读数 / 免打扰时段 / 邮件微信推送
- 无障碍：键盘完整导航 / 减少动画偏好
- 性能优化：长对话虚拟化滚动 / 图片懒加载 / 旧消息内存释放
- 跨页联动：聊天结果发画布 / 生成内容自动存资产 / 启动自动打开上次对话
- 多端适配：手机端聊天适配 / 平板分屏适配 / 桌面客户端
- 数据安全：端到端加密 / GDPR 一键删除 / 导出备份导入恢复
- 运营管理：敏感内容过滤 / 违规对话封禁 / 聊天限速防滥用

### 3. 生图工具 `/image`（核心，8 个 Tab）

> 整体布局：左栏参数面板（340~520px 自适应宽度），右栏结果区。
> Tab 切换有弹簧动画，未开发 Tab 点击提示"开发中"。

#### ① 文生图 ✅
- 提示词输入（最长 2000 字，支持语音输入）
- 常用提示词快捷按钮（一键填入）
- 模型选择器（动态拉取服务端模型配置）
- 画幅比例：1:1 / 2:3 / 3:4 / 4:5 / 9:16 / 21:9 / 3:2 / 4:3 / 5:4 / 16:9 / 原图
- 画质：1K / 2K / 4K（部分模型仅支持 1K）
- 出图数量：1~15 张（部分模型仅支持 1 张）
- 参考图上传（最多 4 张，单张 ≤10MB）
  - 点击上传 / 粘贴板粘贴 / 全局任意位置粘贴自动添加
  - 参考图缩略图 hover 四宫格操作（替换/预览/粘贴替换/删除）
  - 参考图去重（同一张不重复上传）
- 生成排队执行队列（+1 / +5 / +10 / 自定义数量）
- 结果区三种视图
  - 列表视图（缩略图 + 提示词 + 操作栏）
  - 网格视图
  - 大图视图
- 缩略图大小调节（滑块 + Ctrl 滚轮缩放）
- 时间筛选：全部 / 今天 / 近 7 天 / 近 30 天
- 收藏筛选：全部 / 已收藏 / 未收藏
- 结果关键词搜索（只过滤提示词内容）
- 历史记录弹窗（任务状态：成功 / 退款 / 失败 / 保存中 / 进行中）—— 注：应保留所有任务
- 常用提示词收藏（保存提示词、替换、查看）—— 注：需要持久化
- 提示词清空可撤销（toast 带撤销按钮恢复）
- 根据模型能力对参数进行限制
- 生成后自动保存到云端资产（待优化：应自动重试直到成功，不依赖手动点重试）
- 单张结果操作
  - 实时自动保存到云端资产
  - 复用参数重新生成
  - 复制提示词 / 复制图片
  - 引用 / 团队分享
  - 收藏 / 取消收藏
  - 下载 
  - 删除（应移入回收站，到期自动清除）
- 本地文件夹选择（File System Access API，切换保存目录）
- 配额检查（每日免费额度 + 积分余额，不足时按钮置灰并提示）
- 生成按钮直接显示预计积分消耗和当前余额
- 结果区不可折叠
- 提示词超过 40 字自动折叠，可展开收起
- 未保存到云端的结果有红色提示
- 常用提示词管理（可添加标题+内容，点击直接填入）

  **待开发功能：**

  - 生成进度条/预计等待时间
  - 生成失败原因详细提示
  - 批量下载
  - 批量收藏/删除
  - 生成参数一键复制
  - 分享链接生成
  - 模型切换历史记录
  - 提示词模板库（按风格/场景分类）
  - 快捷键支持（Ctrl+Enter 生成）
  - 提示词字数统计
  - 生成完成声音提醒
  - 最近使用的模型置顶
  - 图片加载失败占位图
  - 结果列表自动滚动到底部
  - 生成中禁止重复点击
  - 图片EXIF信息查看

#### ② 融图 🟡
- 上传参考图 + 补充要求 + 强度调节（当前前端模拟）—— 注：强度滑块 UI 未实现，只有状态变量
- 从服务端拉取融图预设提示词
- 支持拖拽上传，上传区棋盘格背景
- hover 显示清除/替换按钮

#### ③ 图片翻译 🟡
- 多图上传（≤10MB/张），原图 - 编辑框 - 译后图三栏对比（当前前端模拟）
- 每张图 240×240 缩略图
- hover 显示重新上传/删除
- 中间编辑框可修改图片里的文字
- 译后图支持重新生成/下载
- 底部"继续添加"按钮追加更多图片
- 底部开始翻译按钮

#### ④~⑧ 其他图像工具 ⬜
- 局部重绘（inpaint）
- 扩图（expand）
- 抠图（cutout）
- 超分辨率（superRes）
- 修复（repair）
- Tab 已显示，点击提示"开发中"

### 4. 视频创作台 `/video` ✅

- 提示词输入
- 模型选择器
- 画幅比例（自动 / 16:9 / 9:16 / 1:1 等）
- 清晰度（240P ~ 1080P）
- 自定义尺寸（宽高）
- 视频时长
- 生成模式
  - 首尾帧模式
  - 全参考图模式
- 参考图上传（最多 7 张，支持拖拽上传、剪贴板直接粘贴）
- 生成音频开关
- 水印开关
- 生成历史记录（本地 IndexedDB 持久化）
- 结果操作：保存到资产 / 下载
- 与 Agent 面板联动（Agent 可直接下视频生成指令）

### 5. 无限画布 `/canvas`（节点式创作工作台）

#### 画布库 `/canvas`
- 项目卡片列表
- 新建画布项目
- 导入画布（ZIP 文件）
- 导出选中项目为 ZIP
- 批量删除 / 删除全部
- 自动打开最近项目（`?mode=recent`）
- Agent 选画布模式（`?mode=choose`）

#### 画布编辑器 `/canvas/:id`

- 顶栏
  - 返回首页
  - 项目标题（点击即可编辑）
  - 新建项目 / 删除项目
  - 导出项目 ZIP / 导入图片
  - 插件管理入口
  - 撤销 / 重做
  - 快捷键说明弹窗
  - 项目列表浮动窗
- 左侧面板（三 Tab，可拖拽调宽度）
  - **画布**：节点列表（按类型筛选、聚焦节点、预览、删除）
  - **资产**：本地资产库（上传/搜索/插入到画布）
  - **提示词**：提示词库（从提示词源加载、查看详情、直接使用）
- 提示词 @ 提及
  - 在输入框打 @ 弹出画布节点选择器
  - 可引用其他图片/视频/音频/文本节点作为参考
  - 支持搜索、键盘上下选择
  - 粘贴图片自动生成节点
- 六种节点类型
  - 文本节点
  - 图片节点（可在节点内直接触发生成）
  - 视频节点
  - 音频节点
  - 配置节点
  - 组节点
- 节点操作
  - 创建 / 删除
  - 复制 / 粘贴（支持从系统剪贴板直接粘贴图片成节点）
  - 编组 / 解组
  - 拖拽移动、自动对齐、吸附到组
  - 连线（节点间资源传递）
  - 自由缩放 / 保持比例
- 图片节点内置编辑工具
  - 裁剪（拖拽裁剪框，8 方向手柄，固定比例预设）
  - 放大/超分辨率（三种算法，目标 1K/2K/4K）
  - 分割（按行列切成多块，可拖拽分割线，最多 12×12，撤销/重做）
  - 蒙版编辑（局部涂抹修改）
  - 角度调整（旋转对话框）
- 编辑操作：撤销 / 重做
- 视图操作：缩放控制、适应视图、小地图
- 底部工具栏
  - 工具切换：选择模式 / 平移模式
  - 快速添加节点：图片 / 视频 / 音频 / 文本 / 配置 / 组
  - 插件扩展节点菜单
  - 上传图片到画布
  - 删除（待优化：应移入回收站，到期自动清除）选中节点 / 清空画布
  - 撤销 / 重做
  - 外观设置：画布背景模式切换、图片信息显示开关
- 项目操作：导出 ZIP / 删除当前项目
- 插件系统
  - 从 URL 安装第三方插件（动态加载 JS）
  - 启用 / 禁用 / 卸载插件
  - 本地插件自动发现（/plugins/index.json）
  - 开发模式插件热加载
  - 插件注册自定义节点类型
- 画布云端同步
  - 项目自动保存到服务端快照
  - 版本号乐观锁（多设备同时编辑时冲突提示）
  - 启动时从云端恢复项目
- 右键菜单、节点悬停工具栏、多选工具栏、顶部工具栏

#### AI Agent 助手（画布右侧浮动小怪兽）
- 对话式操作画布
- 多线程对话历史
- 模型选择 + 推理强度调节（七档：minimal → ultra）
- 工具调用审批（MCP 工具）
  - 三种权限模式：每次询问 / 自动执行 / 完全自动
- 技能（Skills）列表与选择
- 画布上下文感知（引用当前选中节点/内容）
- 附件上传
- Token 用量统计（输入/缓存/输出分开）
- 事件日志查看
- 连接状态管理
- 五个 Tab：对话 / 连接配置 / 历史线程 / 技能 / 日志

#### 画布配置中心（内置能力）🟡
- 模型渠道管理
  - 添加 / 编辑 / 删除渠道
  - 每个渠道：名称、Base URL、API Key、API 格式、模型列表
  - 渠道编辑抽屉
- 默认模型设置
  - 默认图片模型
  - 默认视频模型
  - 默认文本模型
  - 默认音频模型
- 本地代理配置
- WebDAV 网盘同步（🟡 独立版功能，主站未开放）
  - 配置 WebDAV 地址 / 用户名 / 密码 / 目录
  - 测试连接
  - 自动创建远程目录
  - 画布项目数据上传 / 下载
  - Basic Auth 认证，120 秒超时
- 提示词源配置
  - 自定义提示词源（添加 JSON URL 订阅外部提示词库）
  - 内置默认源（不可删除）
  - 每个源：名称 / URL / 主页 / 启用开关
  - 定时刷新间隔：不刷新 / 30分钟 / 1小时 / 6小时 / 24小时
  - 提示词缓存（IndexedDB，1 小时 TTL）
  - 缓存失效时后台静默刷新，先返回旧数据
  - 刷新失败保留旧缓存并记录错误
  - 提示词搜索（标题/内容/标签）、标签筛选、分类筛选、分页
  - 每个提示词带：封面图、参考图、推荐模型/尺寸/数量
- 模型脚本编辑器（自定义模型请求脚本）
- 版本发布弹窗（更新日志）

#### 画布本地资产库（独立于云端资产）
- 六种资产类型：文本 / 图片 / 视频 / 音频 / 文件
- 创建 / 编辑 / 删除 / 恢复资产
- 标签管理
- 关键词搜索 / 类型筛选 / 分页
- 资产包导出 / 导入（ZIP）
- 图片预览与缩略图

---

## 二、个人资产与内容管理

### 6. 资产库 `/assets` ✅

- 类型 Tab：全部 / 图片 / 视频 / 音频 / 文档 / 文件
- 回收站（查看已删除 / 恢复 / 彻底删除）
- 关键词搜索
- 上传文件（图片/视频/音频/文本/PDF/Office 文档，多文件）
  - 小文件直传
  - 大文件分片上传（断点续传）
  - 上传中断后自动恢复
  - 文件去重（内容指纹识别，同文件不重复传）
- 在线预览
  - 图片 / 视频 / 音频直接播放
  - PDF 内嵌预览
  - 文本文件直接显示
- 下载到本地
- 点赞 / 取消点赞
- 评论（查看评论列表 + 发表评论）
- 移入回收站（带确认弹窗）
- 多设备实时同步
  - 轮询服务端事件（10 秒一次）
  - 事件序列游标（每设备独立游标）
  - 处理完事件后确认游标
  - workspace 隔离

### 7. 我的收藏 `/favorites` ✅

- 收藏列表（卡片网格）
- 删除（待优化：应移入回收站，到期自动清除）收藏

---

## 三、账号与团队

### 8. 登录与认证 ✅

- 登录弹窗（全局，未登录点需登录功能时弹出）
- 邮箱密码注册 / 登录
- 第三方登录：Google OAuth / Apple OAuth
- 忘记密码 / 重置密码页
- 本地开发一键登录（`/dev-login`，仅本机可用）
- 登出
- 身份服务：Appwrite（管注册登录）+ 自建业务会话（管余额/资产/任务），通过 JWT 桥接

### 9. 个人中心 `/account`

- **资料页** `/account/profile` ✅
  - 头像（首字母占位）
  - 昵称修改
  - 邮箱（只读）
- **安全页** `/account/security` ✅
  - 修改密码（旧密码 + 新密码 + 确认）
- **账单页** `/account/billing` ✅
  - 当前订阅状态（套餐等级 / 有效期 / 自动续费开关）
  - 申请开票（勾选已支付订单合并开票）
    - 个人抬头 / 企业抬头（企业需税号）
    - 收票邮箱
  - 开票记录（待处理 / 开票中 / 已完成 / 已拒绝）
  - 下载发票 PDF
  - 拒绝原因查看

### 10. 团队系统

- **团队列表** `/teams` ✅
  - 我加入的所有团队
  - 创建新团队
  - 切换当前团队（所有者/管理员标识）
  - 多设备同步刷新
- **团队成员** `/teams/:id/members` ✅
  - 成员列表（姓名 / 邮箱 / 角色 / 加入时间 / 部门职位）
  - 邀请成员（邮箱发送邀请）
  - 设为管理员 / 取消管理员
  - 移除成员（带确认弹窗）

---

## 四、计费与订阅

### 11. 订阅页 `/subscription` ✅

- 三档套餐对比（免费 / Pro / 团队）
- 当前余额 + 会员到期时间
- 购买 / 续费（幂等订单，防重复扣款）
- 每档套餐功能列表

### 12. 钱包 `/wallet` ✅

- 三张概览卡：当前积分余额 / 当前会员等级 / 累计消费
- 每日免费额度（每日生图限额，次日重置）
- 下单前自动询价（quote，预估扣多少积分）
- 四个 Tab
  - **兑换码**：输入 QY-XXXX 兑换码充值
  - **订单**：历史订单列表（订单号 / 套餐 / 金额 / 状态 / 时间）
  - **流水**：积分变动明细（注册赠送 / 兑换 / 会员扣费 / 消费 / 退款 / 邀请奖励 / 管理员调整 / 充值）
  - **邀请有礼**：我的邀请链接 + 已邀请人数
- 自动续费状态查询（是否开启 / 下次扣款时间）
- 退款申请

---

## 五、辅助功能

### 13. 图片翻译 `/translate` 🟡

- 上传图片（PNG/JPG/WebP，≤10MB，多选）
- 选择目标语言（中/英/日/韩/法/德）
- 翻译任务列表（待翻译 / 已完成）

### 14. 产品规划 `/plan` 🟡

- 输入产品名 + 目标背景
- 四阶段模拟流程：市场分析 → 愿景 → 合规 → 导出（当前为动画演示）

### 15. 意见反馈 `/feedback` ✅

- 反馈类型：功能建议 / Bug 报告 / 其他
- 内容输入（支持语音）
- 联系方式
- 提交成功页

### 16. 设置 `/settings`

- **登录设备管理** ✅
  - 最多 3 台设备登录，同时仅 1 台在线
  - 有效设备列表（在线/离线状态、浏览器/系统、最近活动时间）
  - 历史登录记录
  - 踢出设备（带确认）
  - 切换本设备为在线
- 通知 / 外观 / 团队 / 数据 🟡（占位，显示"开发中"）

---

## 六、管理后台（隐藏入口 `/admin-secret-8f3k2x7z`）

### 核心配置组

- **API 与模型**：模型目录管理
  - 模型按能力分组（文本/图片/视频/音频）
  - 每个模型可绑定多个上游渠道（优先级 + 启用开关）
  - 模型显示名 / 可见性 / 排序
  - 积分定价（按请求/输出张数/视频时长/千字符计费）
  - 模型搜索 / 筛选 / 展开折叠
  - 添加 / 编辑 / 删除模型
  - 批量导入模型
  - 从上游服务商拉取模型列表
- **API 密钥管理**：平台 API Key 的创建 / 编辑 / 删除 / 查看调用统计
- **API 调用追踪**：调用记录查询
- **开源项目与数据**：开源数据展示

### 用户与运营组

- **注册用户**：用户列表 / 手动调整积分余额
- **团队与租户**：租户管理
- **计费管理**（7 个 Tab）
  - 统计概览
  - 用户列表（手动调整积分余额）
  - 订单管理（手动完成订单）
  - 兑换码管理（批量生成：数量/面额/类型/有效期）
  - 套餐编辑（名称/价格/功能列表，可保存草稿）
  - 发票管理（标记开票中/完成/拒绝，关联 PDF 地址）
  - 供应商配置（MaiziTech：API Key/余额 Token/Base URL/启用开关，查看余额/Key 限制/可用模型/公告）
  - 系统支付开关
- **供应商管理**：上游服务商余额 / API Key 限制 / 可用模型 / 公告查看
- **开票管理**：全部开票申请审核（通过 / 拒绝 / 上传 PDF）
- **退款管理**：退款记录查看 / 审核

### 系统组

- **审计日志**：管理员操作审计
- **配额审计**：用户额度对账
- **对账与告警**：财务对账 / 告警列表确认
- **运行监控**：CPU 负载 / 内存 / 磁盘 / 运行时长 / 数据库连接状态（30 秒自动刷新）
- **管理员账号**：修改管理员密码（scrypt 哈希存储）

---

## 七、全局基础设施

- **主题**：深色 / 浅色切换（全局状态同步）
- **多语言**：中简 / 中繁 / 英 / 日 / 韩 / 西（共 17 种语言资源包，菜单显示 6 种）
- **语言检测**：根据 IP 提示切换语言
- **语音输入**：全局通用组件（ASR）
  - 本地浏览器端识别（sherpa-onnx WASM 模型，不上传语音到服务器）
  - 首次使用下载模型（加载中状态）
  - 支持 VAD 语音活动检测（自动断句）
  - 支持自动标点
  - 支持实时部分识别（边说边出字）
- **图片查看器**：大图预览、左右切换
- **Toast 反馈**：所有写操作统一提示
- **错误边界**：路由级错误兜底 + 骨架屏加载
- **环境标识**：顶栏显示当前是本地/生产
- **邀请有礼**：顶栏入口弹窗
- **通知铃铛** ⬜（占位）
- **埋点分析**：页面访问与行为追踪（Umami 自托管统计）
- **移动端适配**：侧边栏抽屉式导航（mobile-nav-drawer）
- **全局登录弹窗**：未登录点需登录功能时弹出，登录后保留用户输入内容
- **隐私政策弹窗**（PolicyModal）
- **安全设计**
  - 用户票 HttpOnly Cookie（JS 读不到，防 XSS）
  - AI 供应商 API Key 永不下发前端，统一走后端代理
  - 管理员密码 scrypt 哈希存储

---

## 八、后端服务（`scripts/deploy/api-server.mjs`）

- **用户与认证**
  - 注册 / 登录 / 登出
  - 密码重置
  - 多设备会话管理（3 设备上限，1 台在线）
  - 会话踢出 / 切换在线
- **资产管理**
  - 文件上传 / 下载 / 预览
  - 资产 CRUD / 回收站 / 恢复
  - 点赞 / 评论
  - 多设备事件同步（轮询 + 游标）
- **生图与生视频**
  - 任务提交 / 轮询状态
  - 任务结果存储（COS 对象存储）
  - 失败退款
  - 任务幂等（防止重复扣费）
  - 服务重启后自动恢复中断任务
  - 用 attemptId / providerTaskId 继续轮询上游
  - 已完成但未回传的结果从 recoveryUrl 重新下载存档
  - 分片上传中断后自动恢复
  - 乐观锁防止多实例重复处理
- **计费系统**
  - 套餐列表 / 购买 / 续费
  - 兑换码充值
  - 订单管理
  - 积分流水
  - 邀请奖励
  - 发票申请 / 管理
- **团队系统**
  - 团队 CRUD
  - 成员管理 / 邀请 / 角色
- **反馈提交**
- **管理后台 API**
  - 用户管理 / 租户管理
  - 计费管理 / 套餐配置
  - 审计日志
  - 监控指标（CPU / 内存 / 磁盘）
  - API 调用追踪
- **数据存储**
  - PostgreSQL（计费 / 用户 / 订单 / 发票）
  - COS 对象存储（图片 / 视频 / 音频文件）
  - 本地 JSON 文件（开发模式）

---

## 状态图例

| 标记 | 含义 |
|------|------|
| ✅ | 已接通后端，真实可用 |
| 🟡 | 前端界面已做，逻辑为模拟/占位，未接后端 |
| ⬜ | 入口已预留，功能未开始 |
