# 轻域（QingYu / LitZone）代码级深度分析

> 从源码逐文件阅读，讲清楚每个模块在解决什么矛盾、为什么这么写、数据流怎么走。
> 技术栈：React 19 + Vite 8 + AntD 6 + Tailwind 4 + Zustand + Node.js 原生 HTTP + PostgreSQL + 腾讯云 COS。

---

## 零、全局架构矛盾：一个站，两套身份

整个项目最根本的矛盾是：**用户是谁？**

答案是——两套身份并存：
1. **Appwrite**：管注册、登录、邮箱密码、OAuth（Google/Apple）、密码重置。它是"我是谁"的答案。
2. **自建 PostgreSQL 计费库**：管余额、订单、积分、团队、资产、生图任务。它是"我能用多少、花了多少"的答案。

两套身份通过一个动作缝合：用户登录 Appwrite 后，前端拿 Appwrite 的短期 JWT 去 `/api/billing/login` 换一个 HttpOnly Cookie（`qingyu_session`）。之后所有业务请求靠 Cookie 识别用户，Appwrite 只在登录那一刻出现。

**为什么不合并？** Appwrite 是开源 BaaS，认证做得好但计费/团队/资产不是它的强项；自建 PostgreSQL 灵活但要自己管登录。折中方案就是"认证用 Appwrite，业务用 PG"，用 JWT 做信任桥接。

---

## 一、构建与启动

### `package.json`
- 脚本只有三个：`dev`（vite --host 0.0.0.0）、`build`（tsc -b && vite build）、`preview`。
- 关键依赖：
  - **antd 6**：UI 组件库，全局 toast 用 `App.useApp()`
  - **zustand**：状态管理， persist 中间件做 localStorage 持久化
  - **react-router 7**：路由
  - **localforage**：IndexedDB 封装，生图历史存浏览器本地
  - **i18next**：17 种语言
  - **speech-asr**：语音识别（WebAssembly 跑 sherpa-onnx，完全在浏览器本地跑，不上传服务器）
  - **fflate + file-saver**：画布项目 ZIP 导入导出
  - **nanoid**：生成 ID
  - **streamdown**：Markdown 流式渲染（Agent 对话用）

### `vite.config.ts`
这个文件本身就在讲矛盾——**本地开发绝不能碰生产数据**。
- `localTarget()` 函数强制所有代理目标必须是 `localhost`/`127.0.0.1`，否则直接 throw。
- 没配本地 Appwrite 代理时，`/v1` 路径直接返回 503，不会偷偷转发到生产。
- `/api` 代理到 `127.0.0.1:3001`（自建后端）。
- dev-login 安全：浏览器请求 `/api/dev-login` 时，代理层删掉浏览器自己带的 `X-Qingyu-Local-Dev-Login` 头，只对 loopback 请求者补这个头。防止线上有人伪造本地登录。
- 构建时删掉重复的语音模型 ZIP。
- 别名：`@` → src，`@canvas` → src/canvas。
- 注入 `__APP_VERSION__` 和 `__APP_RELEASES__`（从 VERSION 文件和 CHANGELOG.md 读）。

### `index.html`
- 首屏壳：React 挂载前就显示小怪兽 logo 浮动动画 + "正在准备工作台…"，避免白屏。
- 主题防闪烁脚本：React 挂载前读 localStorage 的主题偏好，直接给 `<html>` 加 `.dark` class。
- Umami 统计：走 nginx 同源反代 `/umami/`。
- 标题："LitZone · 全球视觉设计师、创作者/团队的智能AI创意助手 ~"

### `.env`
- 腾讯云 COS 配置（桶名、区域、SecretId、SecretKey）。生产用环境变量注入。

---

## 二、前端入口与路由

### `src/main.tsx`
- 启动顺序：先 `ensureServerConfig()`（从后端拉模型配置），再渲染 React。
- React Query 配置：重试 1 次，窗口聚焦不重新拉。
- 包裹：StrictMode → QueryClientProvider → BrowserRouter → AntApp → App。

### `src/App.tsx`
- 所有页面 `lazy()` 懒加载，首屏只加载骨架屏。
- 路由分三层：
  1. **公开路由**：`/reset-password`、`/dev-login`
  2. **管理后台**：`/admin-secret-8f3k2x7z`（旧地址全部 301 重定向过来）
  3. **需登录路由**：`ProtectedRoute` 包 `AppLayout`，里面 20+ 个页面
- 画布模块整体懒加载（`@canvas/index`），第一次进 `/canvas` 才拉那个 chunk。
- `*` 兜底全部跳首页。

---

## 三、前端数据层

### `src/lib/api.ts`
- 封装 fetch：baseURL `/api`、30 秒超时、JSON 解析、错误抛 `ApiError`。
- **关键设计**：普通用户票不存 localStorage（安全），只有管理后台的 `admin_token` 才从 localStorage 读。
- 客户请求用 `credentials: "include"`（带 HttpOnly Cookie）。
- `fetchAssetBlob()`：读私有资产文件，token 放 header 不放 URL（防止分享链接泄露凭证）。

### `src/lib/billing.ts`
完整计费 API 客户端，分两块：

**客户接口**（Cookie 认证）：
- `login`：用 Appwrite JWT 换业务会话，同时上报设备信息（OS + 浏览器）
- `me`：拉当前用户（余额/会员等级/到期时间/每日额度已用/总量）
- `quote`：查某个模型某任务要扣多少积分（下单前先询价）
- `plans`：套餐列表
- `createOrder` + `payOrder`：创建订单（带幂等键防重复支付）
- `redeem`：兑换码充值
- `orders` / `transactions`：历史订单和流水
- `invoices`：开票申请和记录
- `invite`：邀请有礼
- `renewalStatus`：自动续费状态

**管理端接口**（Bearer admin_token）：
- 统计、用户列表、手动调余额、完成订单、生成兑换码、套餐编辑、系统设置、供应商余额/Key 限制/模型列表/公告、发票审核。

**设备信息识别**：从 UserAgent 正则匹配 OS（iOS/Android/Windows/macOS/Linux）和浏览器（Edge/Opera/Firefox/Chrome/Safari）。

### `src/lib/generationTasks.ts`
异步生图任务客户端，解决的矛盾是：**同步调上游 AI API 要 30-120 秒，HTTP 超时了怎么办？**

方案：
1. 先 `quote` 询价拿 `creditQuoteId`（预扣积分）
2. POST `/api/generation-tasks` 提交，立即返回 `taskId`（202 Accepted）
3. 前端每 1.5 秒轮询 `GET /api/generation-tasks/:id`
4. 状态机：pending → running → saving → succeeded / failed / refunded
5. 失败/超时后端自动 refunded 退回积分
6. 结果文件通过 `/outputs/:index/content` 流式下载（带 Cookie 鉴权）

**比例换算表**：1K/2K/4K 三档，每个比例对应具体像素值。nano-banana 系列只支持 1K（size 传像素值），gpt-image-2 系列支持 2K/4K（size 传比例字符串，resolution 传档位）。

### `src/stores/useAuthStore.ts`
Zustand + persist，管登录状态。
- 登录方式：邮箱密码、Google OAuth、Apple OAuth、dev-login。
- **设备激活流程**：登录后 `account.createJWT()` 拿 Appwrite 短期令牌 → POST `/api/billing/login`（带 JWT + 邀请码 + 设备信息）→ 后端验证 JWT 后种 HttpOnly Cookie。
- OAuth 回跳处理：sessionStorage 存时间戳，回跳后 10 分钟内自动激活当前设备。
- 三设备限制：后端控制，前端只管调接口。
- `fetchTeams()`：优先读 PostgreSQL 团队，失败兜底读 Appwrite Teams。
- 登出：先调 billing logout（清服务端会话），再删 Appwrite session，再清本地所有票。

### `src/stores/useBillingStore.ts`
- 从 `/api/billing/me` 拉用户余额和会员信息。
- 后端挂了时用本地兜底套餐（免费 0 元/Pro 29 元/团队 99 元），保证页面能看。

### `src/stores/useImageToolsStore.ts`
- 生图工具的本地状态：提示词、比例、画质、数量、模型、结果列表、视图模式、缩略图大小、参考图、队列大小、常用提示词。
- 结果列表存 IndexedDB（localforage），IndexedDB 满了降级 localStorage（只存前 50 条）。

---

## 四、后端服务 `scripts/deploy/api-server.mjs`（3344 行）

### 总体架构
- **零依赖**，只用 Node.js 内置模块（http/https/fs/crypto/stream/path）。
- 开发模式：JSON 文件存储（`api-data/*.json`），原子写入（临时文件 + rename）。
- 生产模式：`BILLING_STORE=postgres` 时动态 import `postgres-billing-store.mjs`，切 PostgreSQL。
- 端口 3001，生产只监听 127.0.0.1（由 nginx 反代）。
- 生产强制 `JWT_SECRET` 非默认值，否则拒绝启动。
- Appwrite 内部地址只允许本机/容器地址，本地开发禁止连远程身份服务。

### 认证体系
- **客户**：HttpOnly Cookie `qingyu_session`，30 天有效期。
- **管理员**：`admin_token`（localStorage），登录 `/api/admin/login` 拿 JWT。
- **JWT 验证**：客户请求进来，从 Cookie 解出 userId 和 sessionId，再查 `isSessionActive()` 确认设备在线。
- **Appwrite JWT 验证**：登录时拿浏览器给的 JWT，内部转发到 Appwrite `/account` 接口验真，不信浏览器自己说的 userId。

### 路由全景（按功能分组）

#### 客户计费路由 `/api/billing/*`
| 路由 | 方法 | 干什么 |
|------|------|--------|
| /login | POST | Appwrite JWT 换业务 Cookie，上报设备信息 |
| /logout | POST | 清 Cookie |
| /me | GET | 当前用户余额/会员/每日额度 |
| /quote | POST | 查模型任务扣多少积分 |
| /plans | GET | 套餐列表 |
| /orders | POST/GET | 创建订单（幂等键防重复）/查我的订单 |
| /redeem | POST | 兑换码充值 |
| /transactions | GET | 积分流水 |
| /invoices | GET/POST | 开票记录/申请开票 |
| /refunds | GET/POST | 退款记录/申请退款 |
| /invite | GET | 邀请码和邀请列表 |
| /renewal-status | GET | 自动续费状态 |

#### 生图/生视频路由 `/api/generation-tasks`
| 路由 | 方法 | 干什么 |
|------|------|--------|
| / | POST | 提交图片生成任务（立即返回 taskId，后台异步跑） |
| /media | POST | 提交视频/音频生成任务 |
| / | GET | 查我的任务列表（最近 50 条） |
| /:id | GET | 查单个任务状态 |
| /:id/outputs/:idx/content | GET | 流式下载生成结果（私有权限校验后 COS 302 跳转） |

**异步 Worker 逻辑**：`setImmediate()` 后台跑，调上游 AI API，成功存 COS + 更新 DB，失败自动退款。有 `reapStaleTasks()` 回收卡住的任务，`enqueueAssetUploadRecovery` 做文件上传恢复。

#### 资产路由
| 路由 | 方法 | 干什么 |
|------|------|--------|
| /assets | GET | 我的资产列表（按类型/关键词筛选） |
| /assets/upload | POST | 单文件上传 |
| /assets/upload-sessions | POST | 分片上传会话（大文件） |
| /assets/:id/content | GET | 下载/预览资产文件 |
| /assets/:id/like | POST | 点赞 |
| /assets/:id/comments | GET/POST | 评论列表/发评论 |
| /assets/:id/recycle | POST | 移入回收站 |
| /favorites | GET | 我的收藏 |
| /content-edits | POST | 内容编辑记录 |

#### 团队路由
| 路由 | 方法 | 干什么 |
|------|------|--------|
| /teams | GET/POST | 团队列表/创建团队 |
| /teams/:id/members | GET/POST | 成员列表/邀请成员 |
| /team-invitations/accept | POST | 接受邀请 |

#### 设备与同步路由
| 路由 | 方法 | 干什么 |
|------|------|--------|
| /account/sessions | GET/DELETE | 我的登录设备列表/踢出设备 |
| /sync/events | GET | 多设备同步事件（轮询） |
| /sync/cursor | POST | 更新同步游标 |

#### 画布路由
| 路由 | 方法 | 干什么 |
|------|------|--------|
| /canvas/projects | GET | 我的画布快照列表 |
| /canvas/projects/snapshot | POST | 保存画布快照 |
| /agent/snapshot | POST | 保存 Agent 对话快照 |

#### 其他客户路由
| 路由 | 方法 | 干什么 |
|------|------|--------|
| /config/public | GET | 公开模型配置（模型列表、支持的比例/画质） |
| /feedback | POST | 提交反馈 |
| /proxy/openai/* | POST | AI 请求代理（服务端注入 API Key） |
| /dev-login | GET | 本地开发一键登录（只允许 loopback） |

#### 管理端路由 `/api/admin/*`
| 路由 | 方法 | 干什么 |
|------|------|--------|
| /login | POST | 管理员登录 |
| /monitor/metrics | GET | CPU/内存/磁盘/运行时间 |
| /billing/stats | GET | 计费总览统计 |
| /billing/users | GET | 用户列表 |
| /billing/users/:id/adjust | POST | 手动调余额 |
| /billing/orders | GET | 全部订单 |
| /billing/orders/:id/complete | POST | 手动完成订单 |
| /billing/codes | GET/POST | 兑换码列表/批量生成 |
| /billing/plans | GET/PUT | 套餐列表/编辑套餐 |
| /billing/settings | GET/PUT | 系统设置 |
| /billing/supplier/* | GET | 供应商余额/Key 限制/模型/公告 |
| /billing/quota/audit | GET | 配额审计 |
| /billing/reconciliation | GET | 对账 |
| /billing/alerts | GET | 告警列表 |
| /billing/invoices | GET | 全部开票申请 |
| /billing/invoices/:id/:action | POST | 审核发票 |
| /billing/refunds | GET | 退款列表 |
| /audits | GET | 审计日志 |
| /api-keys | GET/POST | 平台 API Key 管理 |
| /models-catalog | GET/POST | 模型目录管理 |
| /models-catalog/bulk | POST | 批量导入模型 |
| /fetch-models | POST | 从上游拉取模型列表 |
| /key-stats /key-history | GET | API Key 使用统计 |
| /change-password | POST | 修改管理员密码 |

---

## 五、数据层 `postgres-billing-store.mjs`（3109 行）

这个文件是后端和 PostgreSQL 之间的唯一桥梁，导出约 100 个方法。按领域分：

### 用户与认证
- `ensureAdminAccount`：初始化管理员
- `verifyAppwriteJwt` → 后端自己实现的
- `listSessions` / `isSessionActive` / `activateSession` / `kickSession` / `getSession`：设备会话管理
- `login` / `logout` / `me`：客户登录态

### 计费
- `createUser` / `adjustBalance` / `adjustQuota`
- `createOrder` / `completeOrder` / `listOrders`
- `redeemCode` / `createCodes`
- `createInvoiceRequest` / `updateInvoice`
- `createRefundRequest` / `updateRefund`
- `createCreditQuote`（预扣积分）
- `settleTaskSuccess` / `failTask`（任务完成后结算）
- `reapStaleTasks`（回收超时任务并退款）

### 生图任务
- `createGenerationTask`（幂等键防重复提交）
- `markTaskRunning` / `beginGenerationAttempt` / `completeGenerationAttempt`
- `reserveGeneratedOutput` / `recordGeneratedOutput`
- `getGenerationTask` / `listMyGenerationTasks`
- `listGeneratedOutputs` / `getGenerationOutputRecoveryContext`

### 资产
- `listAssets` / `createAssetFromFile` / `createAssetFromGeneratedOutput`
- `getAssetFile` / `getDerivedFile`
- `deleteAsset` / `restoreAsset`
- `toggleFavorite` / `toggleAssetLike`
- `listAssetComments` / `createAssetComment`
- 分片上传：`createAssetUploadSession` / `recordAssetUploadPart` / `beginAssetUploadCompletion`
- 上传恢复：`claimFileUploadRecoveryJobs` / `finishFileUploadRecoveryJob`

### 团队
- `listTeams` / `createTeam`
- `listTeamMembers` / `inviteToTeam` / `acceptTeamInvitation` / `updateTeamMember`
- `listDepartments` / `createDepartment` / `createJobTitle`

### 模型目录
- `listModelCatalog` / `createModelCatalog` / `updateModelCatalog` / `deleteModelCatalog`
- `listPublicModelCatalogChannels`（前端模型选择器拉这个）
- `listPlatformApiKeys` / `createPlatformApiKey` / `updatePlatformApiKey`

### 画布
- `saveCanvasSnapshot` / `listCanvasSnapshots`
- `saveAgentSnapshot`

### 多设备同步
- `listSyncEvents` / `ackSyncCursor`

### 管理
- `writeAdminAudit` / `listAdminAuditLogs`
- `adminReconciliation` / `listAlerts` / `ackAlert`

---

## 六、数据库迁移 `database/migrations/`（68 个 SQL 文件）

按编号顺序看每个迁移在加什么表/约束：

| 编号 | 内容 | 解决什么问题 |
|------|------|-------------|
| 0001 | 基础表（users/teams/assets） | 建地基 |
| 0002 | 业务域扩展 | 补业务表 |
| 0003 | 操作记录 | 审计日志 |
| 0004 | 资源完整性 | 外键约束 |
| 0005 | 画布 Agent | canvas_agent 表 |
| 0006 | 行级安全 RLS | 数据隔离（用户只能看自己的数据） |
| 0007 | 安全边界 | 权限隔离 |
| 0008 | 业务读边界 | RLS 策略细化 |
| 0009 | RLS 函数执行 | 修 RLS 权限 |
| 0010 | 计费一致性 | 余额校验约束 |
| 0011 | Agent 治理 | Agent 使用审计 |
| 0012 | 备份目录 | backup_catalog |
| 0013 | 会话实时 | sessions 表 |
| 0014 | 组织生命周期 | 团队删除级联 |
| 0015 | 工作区关联完整性 | 外键 |
| 0016 | 提示词链接触发器修复 | bug fix |
| 0017 | 方案版本一致性 | 版本号约束 |
| 0018 | 资产分享目标完整性 | 外键 |
| 0019 | 支付幂等 | idempotency_key 唯一约束 |
| 0020 | 推荐码和兑换码 | invite/redeem |
| 0021 | 三设备上限 | sessions 数量约束 |
| 0022 | 单设备在线 | 同一时间只一个 active session |
| 0023 | 平台 API Key | 供应商密钥表 |
| 0024 | API Key 模型长度 | 字段长度调整 |
| 0025 | 加密平台 API Key | 密钥加密存储 |
| 0026 | 画布外部 Key | canvas 自带的渠道 Key |
| 0027 | Agent 快照同步 | agent_snapshots |
| 0028 | 用量配额关联 | usage → quota 关联 |
| 0029 | 每日免费额度 | daily_quota |
| 0030 | 用量对账 | reconciliation |
| 0031 | 退款 | refunds 表 |
| 0032 | 支付事件 | payment_events |
| 0033 | 备份副本 | backup_copies |
| 0034 | 开票申请 | invoice_requests |
| 0035 | 提示词模板标签 | prompt_template_tags |
| 0036-0040 | 范围完整性约束 | 一系列外键/CHECK 约束 |
| 0041 | API Key 明文防护 | 禁止明文返回 |
| 0042-0043 | 会话硬上限守卫 | 触发器保证不超 3 台 |
| 0044 | 退款金额守卫 | 触发器 |
| 0045 | 配额幂等范围守卫 | 触发器 |
| 0046 | 异步生图任务 | generation_tasks 核心表 |
| 0047 | 任务幂等锁 | idempotency_key 唯一 |
| 0048 | 数据库角色 schema | 权限分离 |
| 0049 | 对账漂移和审计查询 | 视图 |
| 0050 | 反馈提交 | feedback_submissions |
| 0051 | 管理员目录设置 | admin_catalog_settings |
| 0052-0053 | 用量完成指标 | completion 统计 |
| 0054 | 管理员账号 | admin_accounts |
| 0055 | 会话在线守卫 | 触发器 |
| 0056 | 支付事件重试和多回执 | payment_events 增强 |
| 0057 | 退款关联收据 | refunds → receipts |
| 0058 | COS 文件元数据和关联 | cos_file_metadata |
| 0059 | COS 上传会话和检查 | upload_sessions |
| 0060 | 生成尝试和输出槽位 | generation_attempts / output_slots |
| 0061 | 内容编辑、节点资产、文件任务 | content_edits / node_assets / file_jobs |
| 0062 | 订阅续订生命周期 | subscription_renewals |
| 0063 | 模型目录渠道模型 | model_catalog_channel_models |
| 0064 | 文件任务 Worker 函数 | 触发器函数 |
| 0065 | 生成输出恢复 | generation_output_recovery |
| 0066 | 管理员登录尝试限制 | rate limiting |
| 0067 | 生成媒体失败记账 | failure accounting |
| 0068 | 模型积分定价 | model_credit_pricing |

**一句话总结迁移演进**：从建表 → RLS 安全隔离 → 计费一致性约束 → 异步任务系统 → 支付幂等 → 上传恢复 → 触发器守卫。每一个迁移都在堵一个具体的漏洞。

---

## 六、画布模块（src/canvas）

这个模块是从一个开源的"无限画布 AI 创作台"模板移植过来的，现在被主站原生集成（不再是 iframe）。它解决的矛盾是：**让用户在一张无限大的画布上，用节点连线的方式做 AI 创作，而不是填表单。**

### 6.1 核心数据模型 `types/canvas.ts`

画布的一切围绕三种实体：

**Node（节点）**：
- 六种内置类型：image / text / config / video / audio / group
- 插件节点用 `<pluginId>:<name>` 字符串扩展
- 每个节点有 position(x,y)、width、height、metadata
- metadata 是个大杂烩：提示词、模型名、尺寸、画质、生成数量、状态(idle/loading/success/error)、引用图列表、生成结果数组、视频任务ID 等
- 图片节点可以有多张生成结果（images 数组），primaryImageId 标记当前显示哪张
- 文本节点同理（texts 数组）

**Connection（连线）**：fromNodeId → toNodeId，表示数据流向（比如上游图片作为下游图生图的参考图）。

**AssistantSession（Agent 对话）**：
- 一个画布项目可以有多轮 Agent 对话
- 每条消息有 role(user/assistant/system/tool/error)、text、references（引用了哪些节点）
- Agent 可以"看到"画布上的节点内容

### 6.2 配置中心 `stores/use-config-store.ts`

解决的矛盾是：**模型太多，怎么组织？**

方案是"渠道（Channel）"概念：
- 每个渠道 = 一个 API 提供商（baseUrl + apiKey + apiFormat）
- apiFormat 两种：openai 格式 或 gemini 格式
- 一个渠道下挂多个模型
- 模型 ID 格式：`渠道ID::模型名`，这样前端知道该往哪个渠道发请求
- 四种能力各有默认模型：imageModel / videoModel / textModel / audioModel
- 支持本地代理模式（`channelMode: "local"`），可以走本地代理（127.0.0.1:23210）
- 配置持久化到 localStorage

**主站模式的关键变化**（`server-config-bootstrap.ts`）：
- 前端启动时从 `/api/config/public` 拉后端配置
- apiKey 统一设为 `"proxy"`——**真正的 API Key 不下发前端**，所有 AI 请求走后端代理
- 后端返回多个渠道，前端自动按 capability 匹配默认模型
- 补充了 seedance/omni/h3 等视频模型关键字识别

### 6.3 画布状态 `stores/canvas/use-canvas-store.ts`

解决的矛盾是：**画布数据既要存本地（快），又要同步到云端（多设备）。**

方案：
- 项目数据存 IndexedDB（localforage），启动快
- 每次变更防抖 500ms 后 POST 到 `/api/canvas/projects/snapshot`
- **乐观锁**：每个项目有 serverVersion，后端返回冲突时前端弹"版本冲突"提示
- 写入队列串行化（canvasWriteQueue），防止并发写乱序
- 首次启动从 `/api/canvas/projects` 拉云端快照，和本地合并

### 6.4 文件上传 `services/file-storage.ts`

解决的矛盾是：**大文件上传到 COS 慢，断网了怎么办？**

方案：
- **幂等上传**：文件前 1MB + 后 1MB 做 SHA-256 指纹，生成唯一 sessionKey
- 同一份文件重复上传时，先查 session 是否已经完成，完成了直接返回 assetId
- **分片上传**：大文件走 upload-sessions API，断点续传
- 上传会话状态存 IndexedDB，刷新页面后能恢复
- 本地 IndexedDB 缓存（media_files store），生成的图先存本地再传云端

### 6.5 AI 请求 `services/api/image.ts`（941 行）

这是画布模块最大的服务文件，解决的矛盾是：**上游 AI API 格式五花八门，前端要统一。**

它做了：
- 同时支持 OpenAI 格式和 Gemini 格式的请求/响应转换
- 支持 SSE 流式响应（Agent 对话逐字输出）
- 支持 function calling（工具调用）——Agent 可以调画布操作工具
- 支持多模态输入（文本 + 图片 URL）
- 模型插件机制（model-plugin.ts，961 行）——可以自定义请求/响应处理脚本
- 图片参考图提示词自动构建（把参考图拼进提示词）

### 6.6 画布编辑器 `pages/canvas/project.tsx`（3288 行）

这是整个项目最大的文件。它是一个基于 React Flow 的节点编辑器：
- 无限画布（缩放/平移/小地图）
- 拖拽创建节点、连线
- 节点内直接触发生成（图片节点上点"生成"按钮）
- 生成结果在节点内展示，多图切换
- 右键菜单、悬停工具栏、多选工具栏
- 撤销/重做
- 插件系统（自定义节点类型）

### 6.7 Agent 面板 `stores/use-agent-store.ts`

解决的矛盾是：**用户不想手动点按钮，想用自然语言指挥 AI 操作画布。**

方案是接一个独立的 Agent 后端服务（默认 `127.0.0.1:17371`），用 SSE 流式通信。这个 Agent 本质上是一个 Codex 风格的编程 Agent 壳，被改造成了画布操作 Agent：

- **多线程对话**：threads 列表，可切换历史会话
- **MCP 工具**：Agent 可以调外部工具（文件读写、浏览器等），每个工具调用需要用户审批
- **三种权限模式**：
  - `request`：每次工具调用都问用户
  - `automatic`：自动执行
  - `full`：完全自动
- **画布上下文感知**：Agent 能看到当前画布的节点快照，可以通过 ops 指令修改画布（增删节点、改提示词、触发生成）
- **模型选择**：支持切换不同 LLM，调节推理强度（minimal → ultra 七档）
- **五种 Tab**：chat（对话）/ setup（连接配置）/ history（历史线程）/ skills（技能）/ log（事件日志）
- **Token 用量**：输入/缓存/输出分开统计
- 连接配置（URL + Token）存 localStorage

### 6.8 插件系统 `lib/canvas/plugin-loader.ts`

解决的矛盾是：**画布内置节点不够用，第三方怎么扩展？**

方案是运行时动态加载 JS 插件：
- 从 URL fetch 插件源码 → Blob URL → `import()` 动态执行
- 插件导出节点定义，注册到 node-registry，画布创建菜单里就多了自定义节点
- 插件可以注入 CSS、注册设置面板
- 三种插件来源：
  1. **本地插件**：`/plugins/index.json` 发现，默认禁用，用户手动开
  2. **远程插件**：用户输入 URL 安装，源码缓存到 IndexedDB
  3. **开发插件**：`VITE_DEV_PLUGINS` 环境变量，每次启动热加载

---

## 七、安全设计细节

### 7.1 管理员密码 `admin-password.mjs`

- 新密码用 **scrypt** 哈希（N=16384, r=8, p=5），64MB 内存上限
- 老密码兼容 SHA-256+salt，登录成功后自动提示升级
- 验证用 `crypto.timingSafeEqual`，防时序攻击

### 7.2 对象存储 `object-store.mjs`（腾讯云 COS）

- 生产/测试用不同桶（`COS_BUCKET_PROD` / `COS_BUCKET_TEST`）
- 上传上限 5GB，分片上传每片 5MB，失败重试 3 次
- **流式上传三重校验**：声明大小 vs 实际大小 vs SHA-256 哈希
- 路径安全：拒绝 `/` 开头和 `..` 路径，防路径穿越
- 读取用 CDN 签名 URL（302 跳转，不经过后端代理）

### 7.3 认证安全

- **客户票**：HttpOnly Cookie，JS 读不到，防 XSS 窃取
- **管理员票**：localStorage，管理后台专用
- **API Key**：永远不下发前端，前端统一设为 `"proxy"`，所有 AI 请求走后端代理
- **JWT**：生产强制非默认 secret，启动时校验
- **dev-login**：只允许 loopback 请求，代理层删掉浏览器伪造的头
- **CORS**：开发环境白名单 localhost:5173，生产由 nginx 同源处理

---

## 七、前端公共组件

### `components/layout/AppLayout.tsx`（503 行）
主框架：左侧边栏导航 + 顶栏 + Outlet。
- 侧边栏：首页/AI对话/生图/视频/画布/资产/收藏/计划/团队
- 顶栏：全局搜索、通知、邀请有礼、积分商城、订阅、用户头像下拉
- 底部：主题切换、语言切换
- 未登录时点需登录功能 → 弹 AuthModal

### `components/AuthModal.tsx`（287 行）
全局登录弹窗，Tab 切换登录/注册，支持 Google OAuth 按钮。

### `components/speech-input-button.tsx`（47 行）
语音输入按钮，调用 speech-asr（WebAssembly 本地 ASR）。

### `components/model-picker.tsx`（50 行）
模型选择下拉框，从 useConfigStore 读模型列表。

### `components/states/`
- RouteSkeleton：路由切换时的骨架屏（grid/canvas/chat/list/settings/workspace 六种变体）
- ErrorState：错误兜底页
- RouteErrorBoundary：路由级错误边界
- AppBootSplash：启动闪屏

---

## 八、部署架构

### `scripts/deploy/api-server.mjs`
零依赖 Node.js HTTP 服务，端口 3001，生产只监听 127.0.0.1。
- 开发模式：JSON 文件存储（原子写入）
- 生产模式：PostgreSQL（postgres-billing-store.mjs）
- 对象存储：腾讯云 COS（object-store.mjs）
- 部署方式：scp 上传 `~/qingyu-api/api-server.mjs`，systemd 管理进程

### `docker-compose.umami.yml`
Umami 网站统计（自托管分析），容器内跑。

### Nginx 配置
- 静态文件：前端 build 产物
- `/api` → 127.0.0.1:3001（后端）
- `/v1` → Appwrite（认证）
- `/umami/` → Umami 统计
- `/models/` → 静态语音模型文件

---

## （继续补充中……）

> 以下模块待读完后补入：
> - Agent 面板详细逻辑
> - 画布节点交互细节
> - 各页面具体业务逻辑
> - 测试脚本体系
