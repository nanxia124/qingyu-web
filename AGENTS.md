# 开发规则

> **⚠️ 最高优先级——动手授权的硬门槛，任何其他规则都不能压过这条**
>
> 1. **改代码前必须先问**：任何涉及修改/新增/删除文件的操作，先用大白话说清楚要做什么、因为什么、达成什么目的，并列出具体要改哪些文件、每个文件改什么、影响范围。
> 2. **选定方向 ≠ 授权动手**：用户说"按C来""选A"只代表选了方向；必须等用户明确回复"可以开始""开始吧""动手吧"这类指令，才允许动文件。没有这句话，一个字都不写。
> 3. **只读调查可以正常做**（读文件、查状态、Grep/Glob），但调查完必须回到第 1 条先发开工申请，不能顺手就改。
> 4. 修 bug、报错、紧急情况、用户催促，同样适用，不得跳过。

## 全局 UI 规则
- **禁止描边**：所有项目默认不允许出现 border/描边，除非用户特意要求
- **暗色风格**：保持现有暗色主题，不要主动改配色方案

## 开发流程
- **先复述理解，确认后再动手**：用户提出任何要求后，先口头复述我对需求的理解、要做的事和范围，等用户确认无误后，再开始写代码、修改或进行其他操作；不要在未对齐前就直接动手
- **汇报必须说人话**：调查完、排查完、给方案时，先用大白话讲清楚"发生了什么、问题在哪、要做什么、有啥后果"，让不懂代码的人也能看明白；文件名、函数名、行号这些技术细节放在大白话后面当补充，不能一上来就甩一堆黑话。需要用户拍板的事，要讲清"几个选项分别意味着什么"，不能只列名词让用户猜
- **先调查再动手**：没有调查就没有发言权。改代码前必须先读文件、查清楚问题在哪、确认要改的位置，不要凭猜测直接改
- **先检查再改**：动手改代码前，先读取相关文件、确认当前状态、明确要改的位置，不要凭记忆直接替换
- **小步提交**：每次只改一小块，改完立刻验证，不要大改后一次性提交
- **边改边验证**：不要一次改一大堆代码。每改一小部分（一个函数、一个组件、一个接口），就立刻跑一遍检查（TypeScript 编译、页面刷新），没问题再继续改下一块。出了问题也能快速定位到刚改的那几行
- **本地/生产隔离**：严格区分本地 localhost 和生产 litzone.art，不要混到一起

## 部署流程
- **前端改动**（src/ 下的 tsx/ts/css）：`git push origin main` → GitHub Actions 自动构建并部署到 nginx
- **后端改动**（scripts/deploy/api-server.mjs）：push 后还需手动 scp 上传到生产服务器并重启 node 进程
- 生产服务器：`ubuntu@43.160.249.6`，API 路径 `~/qingyu-api/api-server.mjs`

## 代码规范
- 组件命名清晰，不要用缩写
- 关键操作（删除、关闭、开启）必须有确认弹窗
- 头像、图标等资源优先用默认映射，不要每个都手动配置
- **所有写操作必须有 toast 反馈**：保存、删除、切换、提交等写操作完成后，成功/失败都要弹 toast；toast 统一用 antd `App.useApp()` 的 `message`，不要混用其他组件

## 文档规范
- 写 `docs/` 下任何文档前，必须先读 `docs/README.md` 的"写白话的规矩"和"改动后怎么写入规范"
- 流程：改完代码先让用户测试 → 用户确认没问题后，再主动问"要不要写入规范？" → 用户回"可以写入"后，才按"白话大字+专业小字"格式写入对应文档
- **需求文档状态要同步更新**：需求文档里"现在长什么样"一节写的是当前进度，做完验证后必须同步更新成实际完成的样子，不能一直留着"还没开始做""下一步做XX"这种过时描述；更新时机跟写入规范一起，用户回"可以写入"后一起改

## 计费额度
- 免费生图额度以 `app.daily_usage_reservations` 中当天 `image_gen` 的 `reserved` 和 `committed` 记录为准；前端展示和禁用按钮必须使用 `/api/billing/me` 返回的 `dailyUsed` / `dailyLimit`。
