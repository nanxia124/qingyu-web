# 开发规则

## 铁律（任何其他规则不得压过）

1. **只在项目根目录内活动**：不许查看、修改项目目录以外的任何文件。
2. **动手前必须拿到明确指令**：用户提需求后，先用大白话说清要做什么、改哪些文件、每个文件改什么、影响范围；用户说"开始/动手吧"之前，一个字都不改。用户说"选 A""按 C 来"只代表选方向，不算授权。
3. **看不懂就问，不许猜**：有歧义、有争议、没理解用户想法，立刻停下来问清楚再继续，不要自作主张。修 bug、报错、紧急情况也不例外。
4. **只读调查随时可以做**：读文件、查状态、Grep/Glob 随便做，但做完必须回到第 2 条重新申请开工，不能顺手就改。
5. **严禁打比方**：解释问题、汇报进度、给方案时，直接说事实、说后果、说界面上能看到什么。不许用"就像""好比""相当于""打个比方"这种类比句。觉得用户听不懂，就把句子拆短、换词重说，不许编比喻。

## 界面规矩

- 禁止描边（border），除非用户特意要求。
- 保持双主题，不主动改配色方案。
- 界面上给用户看的文字必须走多语言（i18n），不能硬写死中文；新增文案要在语言文件里加，不要在组件里写死。

## 干活流程

- **说人话**：给方案、汇报进度、说问题时，先用大白话讲清"用户在界面上能看到什么、为什么之前坏了、有啥后果"；服务器命令、文件路径、函数名、接口名一律不写进正文，非写不可就塞最后"技术备注"里。需要用户拍板时，讲清几个选项分别意味着什么，别只甩名词。
- **讲方案必须带代价**：给方案、给选项、做技术决定时，优点和缺点分条列出来，有几条列几条，不限数量；安全隐患没有就明写"没有"，不许空着、不许只说好处。列完后必须给一个最佳推荐。纯事实性问答不用硬凑。
- **先看现状再改**：动手前先读相关文件、确认当前状态，不凭记忆替换。
- **小步快跑**：每次只改一小块（一个函数/组件/接口），改完立刻跑一遍检查（TypeScript 编译、页面刷新），没问题再继续。
- **AI 能做的直接做**：跑命令、改代码、查日志、杀进程、重启服务这类 AI 能干的事，直接做，不要列"你这么做 1/2/3"让用户手动操作。只有 AI 确实干不了的才找用户，比如要登录密码、要扫码、要付钱、要用户在浏览器里点按钮、要用户提供我看不到的信息。删数据、上线、改核心逻辑这类关键操作，做之前还是要先跟用户确认。
- **本地生产分开**：严格区分本地 localhost 和生产 litzone.art，不混。
- **"统一" = 全项目逐行审**：用户说要统一某样东西时，必须把项目所有相关文件逐行读一遍，不许跳过、不许漏。

## 代码规矩

- 组件命名清晰，不用缩写。
- 删除、关闭、开启这类关键操作，必须弹确认框。
- 头像、图标用默认映射，别每个单独配。
- 所有写操作（保存、删除、切换、提交）必须有 toast 反馈；toast 统一用 antd `App.useApp()` 的 `message`，别混用其他组件。

## 部署

- 前端改动（src/ 下的 tsx/ts/css）：`git push origin main` → GitHub Actions 自动构建并部署到 nginx。
- 后端改动（scripts/deploy/api-server.mjs）：push 后还要手动 scp 到生产服务器并重启 node 进程。
- 生产服务器：`ubuntu@43.160.249.6`，API 路径 `~/qingyu-api/api-server.mjs`。

## 文档规矩

- 写 `docs/` 下任何文档前，先读 `docs/README.md` 的"写白话的规矩"和"改动后怎么写入规范"。
- 代码改完先让用户测试；用户确认没问题后，主动问"要不要写入规范？"，用户说"可以写入"才按"白话大字+专业小字"格式写。
- 需求文档里"现在长什么样"必须跟着进度同步更新，不能留着"还没开始""下一步做 XX"这种过时话。

## 计费额度

- 新用户一次性赠分数由管理员后台设置；设为 0 时暂停赠分。设置只影响保存后注册的新用户，已到账积分不变。
