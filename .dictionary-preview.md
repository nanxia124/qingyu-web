# 轻域 PostgreSQL 字段数据字典


1. **<span style="font-size:18px;color:#fff">这是一本"房间清单+物品目录"——系统里115张表、1140个字段，每件叫什么名字、什么规格、能不能空着、用来干嘛，全记在这本目录里。</span>**<br>
   <span style="display:inline-block;font-size:12px;color:#666;transform:scale(0.35);transform-origin:left top;line-height:1">纯技术参考：每张表每个字段叫什么、什么类型、能不能空、干嘛用的。</span>


2. **<span style="font-size:18px;color:#fff">以后要加东西或改东西的人，先翻这本目录，看看有没有现成的格子可以放，别重复开新房间。</span>**<br>
   <span style="display:inline-block;font-size:12px;color:#666;transform:scale(0.35);transform-origin:left top;line-height:1">给开发和 AI 查表用，改字段前先看这个。</span>


3. **<span style="font-size:18px;color:#fff">现在已经整理了115张表、1140个字段，以后每改一个房间都得同步更新这本目录，不然目录和实际房间就对不上了。</span>**<br>
   <span style="display:inline-block;font-size:12px;color:#666;transform:scale(0.35);transform-origin:left top;line-height:1">按项目迁移重建 115张表、1140个字段，以后改表必须同步更新。</span>


4. **<span style="font-size:18px;color:#fff">这些物品按用途分了六大类——你的账号和登录设备、你的个人空间和团队、你的套餐和花钱记录、你的额度和用量、你生成的图片和作品、系统管理和备份。</span>**<br>
   <span style="display:inline-block;font-size:12px;color:#666;transform:scale(0.35);transform-origin:left top;line-height:1">用户与安全、空间与多租户、订阅与计费、额度与用量、内容与生成、平台运维。</span>

> 来源：项目 `database/migrations` 中的数据库更新文件，从空库重建得到的 `app` schema。本文描述项目当前定义的结构，不代表线上数据库实时结构，也不替代迁移文件。

### 这是什么
纯技术参考：每张表每个字段叫什么、什么类型、能不能空、干嘛用的。

### 用户怎么用
给开发和 AI 查表用，改字段前先看这个。

### 现在长什么样
按项目迁移重建 115张表、1140个字段，以后改表必须同步更新。

## 数据分层

- **用户与安全**：账号、设备、会话、同意和安全事件。
- **空间与多租户**：个人空间、团队空间和组织成员关系。所有业务数据必须沿 `workspace_id` 或专用授权关系隔离。
- **订阅与计费**：套餐、订单、支付、订阅、退款、发票。支付事实不能用前端状态代替。
- **额度与用量**：预占、结算、释放、流水和对账。余额是可重算结果，流水才是事实。
- **内容与生成**：生成任务、结果、资产、文件、收藏、点赞和评论。
- **平台运维**：模型目录、渠道、备份、恢复、管理员和系统设置。

## 字段约定

| 列 | 含义 |
|---|---|
| 表 | PostgreSQL 表或视图名 |
| 字段 | 数据库真实列名，代码中保持一致 |
| 类型 | PostgreSQL 信息架构返回类型 |
| 可空 | YES 表示允许 NULL；NO 表示必须有值 |
| 默认值 | 数据库默认表达式；— 表示没有默认值 |
| 用途 | 通俗解释；未建立专门说明时按字段名归纳，标注“推断” |

### `access_policies`（权限）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `resource_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `effect` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `conditions` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `enabled` | boolean | NO | `true` | 业务字段（推断，需按该表业务流程补充） |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `admin_accounts`（平台运维）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `username` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `password_hash` | character varying | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `status` | text | NO | `'active'::text` | 生命周期状态；由表级约束限定枚举 |
| `last_login_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `admin_audit_logs`（平台运维）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `actor` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `action` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `target_type` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `target_id` | character varying | YES | `—` | 关联编号（推断） |
| `summary` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `metadata` | jsonb | NO | `'{}'::jsonb` | 扩展元数据 JSON；不能存权限事实或密钥 |
| `ip` | inet | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `admin_login_attempt_limits`（平台运维）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `account_key` | character | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `attempt_count` | smallint | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `window_started_at` | timestamp with time zone | NO | `—` | 时间字段（UTC，具体含义按表内状态解释） |

### `agent_approvals`（Agent）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `turn_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `request_key` | character varying | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `action_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `input_hash` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'pending'::text` | 生命周期状态；由表级约束限定枚举 |
| `requested_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `decided_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `expires_at` | timestamp with time zone | NO | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `decided_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `decision_reason` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `agent_attachments`（Agent）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `message_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `file_id` | uuid | NO | `—` | 关联编号（推断） |
| `attachment_role` | character varying | NO | `'input'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `agent_event_logs`（Agent）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `thread_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `event_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `payload` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `occurred_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |
| `external_event_id` | character varying | YES | `—` | 关联编号（推断） |

### `agent_messages`（Agent）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `thread_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `external_item_id` | character varying | YES | `—` | 关联编号（推断） |
| `role` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `content` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `attachments` | jsonb | NO | `'[]'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `canvas_references` | jsonb | NO | `'[]'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `usage` | jsonb | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `agent_threads`（Agent）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `user_id` | uuid | YES | `—` | 关联用户内部编号 |
| `external_thread_id` | character varying | YES | `—` | 关联编号（推断） |
| `title` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `workspace_path` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'active'::text` | 生命周期状态；由表级约束限定枚举 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `agent_tool_calls`（Agent）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `turn_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `call_key` | character varying | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `tool_name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `input_hash` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'queued'::text` | 生命周期状态；由表级约束限定枚举 |
| `result` | jsonb | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `external_request_id` | character varying | YES | `—` | 关联编号（推断） |
| `started_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `finished_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `agent_turns`（Agent）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `thread_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `turn_key` | character varying | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `status` | text | NO | `'queued'::text` | 生命周期状态；由表级约束限定枚举 |
| `request_hash` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `model` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `pricing_snapshot` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `lease_token` | uuid | YES | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `lease_expires_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `attempt_count` | integer | NO | `0` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `error_code` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `error_message` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `started_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `finished_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `ai_channels`（模型与渠道）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_id` | uuid | YES | `—` | 关联编号（推断） |
| `name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `base_url` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `secret_ref` | text | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `model_allowlist` | jsonb | NO | `'[]'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `max_concurrency` | integer | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'active'::text` | 生命周期状态；由表级约束限定枚举 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `ai_models`（模型与渠道）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `provider_id` | uuid | NO | `—` | 关联编号（推断） |
| `code` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `display_name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `capabilities` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `limits` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `pricing` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `enabled` | boolean | NO | `true` | 业务字段（推断，需按该表业务流程补充） |
| `version` | integer | NO | `1` | 乐观锁或配置版本号 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `ai_providers`（模型与渠道）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `code` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `base_url` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `capabilities` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `enabled` | boolean | NO | `true` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `asset_comments`（资产与互动）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `asset_id` | uuid | NO | `—` | 关联编号（推断） |
| `parent_id` | uuid | YES | `—` | 关联编号（推断） |
| `author_user_id` | uuid | YES | `—` | 关联编号（推断） |
| `content` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `moderation_status` | text | NO | `'pending'::text` | 业务字段（推断，需按该表业务流程补充） |
| `edited_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `deleted_at` | timestamp with time zone | YES | `—` | 软删除时间；为空表示未删除 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |

### `asset_files`（资产与互动）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `asset_version_id` | uuid | NO | `—` | 关联编号（推断） |
| `file_id` | uuid | NO | `—` | 关联编号（推断） |
| `role` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |

### `asset_likes`（资产与互动）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `asset_id` | uuid | NO | `—` | 关联编号（推断） |
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |

### `asset_references`（资产与互动）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `source_asset_id` | uuid | NO | `—` | 关联编号（推断） |
| `target_asset_id` | uuid | NO | `—` | 关联编号（推断） |
| `referenced_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `purpose` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |

### `asset_shares`（资产与互动）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `asset_id` | uuid | NO | `—` | 关联编号（推断） |
| `shared_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `target_type` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `target_id` | uuid | YES | `—` | 关联编号（推断） |
| `token_hash` | text | YES | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `permission` | text | NO | `'view'::text` | 业务字段（推断，需按该表业务流程补充） |
| `expires_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `revoked_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |

### `asset_versions`（资产与互动）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `asset_id` | uuid | NO | `—` | 关联编号（推断） |
| `version_no` | integer | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `metadata` | jsonb | NO | `'{}'::jsonb` | 扩展元数据 JSON；不能存权限事实或密钥 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |

### `assets`（资产与互动）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `source_generation_id` | uuid | YES | `—` | 关联编号（推断） |
| `asset_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `title` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `description` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `visibility` | text | NO | `'private'::text` | 业务字段（推断，需按该表业务流程补充） |
| `moderation_status` | text | NO | `'pending'::text` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'active'::text` | 生命周期状态；由表级约束限定枚举 |
| `version` | bigint | NO | `1` | 乐观锁或配置版本号 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |
| `deleted_at` | timestamp with time zone | YES | `—` | 软删除时间；为空表示未删除 |

### `audit_logs`（审计与安全）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `actor_user_id` | uuid | YES | `—` | 关联编号（推断） |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `action` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `resource_type` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `resource_id` | uuid | YES | `—` | 关联编号（推断） |
| `result` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `request_id` | character varying | YES | `—` | 关联编号（推断） |
| `before_summary` | jsonb | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `after_summary` | jsonb | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `occurred_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |

### `backup_copies`（备份与恢复）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `backup_id` | uuid | NO | `—` | 关联编号（推断） |
| `storage_provider` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `storage_key` | text | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `status` | text | NO | `'started'::text` | 生命周期状态；由表级约束限定枚举 |
| `checksum_sha256` | character | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `size_bytes` | bigint | YES | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `uploaded_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `verified_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `expires_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `error_message` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `backup_runs`（备份与恢复）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `backup_type` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `scope` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'started'::text` | 生命周期状态；由表级约束限定枚举 |
| `source_database` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `schema_version` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `storage_provider` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `storage_key` | text | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `checksum_sha256` | character | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `size_bytes` | bigint | YES | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `encryption_key_version` | character varying | YES | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `base_backup_id` | uuid | YES | `—` | 关联编号（推断） |
| `wal_start_lsn` | pg_lsn | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `wal_end_lsn` | pg_lsn | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `started_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |
| `finished_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `verified_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `expires_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `error_code` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `error_message` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `billing_cycles`（其他业务表）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `subscription_id` | uuid | NO | `—` | 关联编号（推断） |
| `sequence` | integer | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `period_start` | timestamp with time zone | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `period_end` | timestamp with time zone | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `due_at` | timestamp with time zone | NO | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `amount_minor` | bigint | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `currency` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `plan_id` | uuid | NO | `—` | 关联编号（推断） |
| `plan_version_id` | uuid | NO | `—` | 关联编号（推断） |
| `billing_interval` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `billing_timezone` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `authorized_max_amount_minor` | bigint | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `mandate_id` | uuid | NO | `—` | 关联编号（推断） |
| `order_id` | uuid | YES | `—` | 关联编号（推断） |
| `collection_owner` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_account_id` | character varying | NO | `—` | 关联编号（推断） |
| `environment` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_cycle_id` | character varying | YES | `—` | 关联编号（推断） |
| `status` | text | NO | `'scheduled'::text` | 生命周期状态；由表级约束限定枚举 |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `canvas_chat_messages`（画布）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `session_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `message_key` | character varying | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `role` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `content` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `detail` | jsonb | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `canvas_chat_sessions`（画布）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `project_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `session_key` | character varying | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `title` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `canvas_connections`（画布）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `project_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `connection_key` | character varying | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `from_node_id` | uuid | NO | `—` | 关联编号（推断） |
| `to_node_id` | uuid | NO | `—` | 关联编号（推断） |
| `metadata` | jsonb | NO | `'{}'::jsonb` | 扩展元数据 JSON；不能存权限事实或密钥 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `canvas_node_asset_uses`（画布）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `project_id` | uuid | NO | `—` | 关联编号（推断） |
| `node_id` | uuid | NO | `—` | 关联编号（推断） |
| `slot_key` | character varying | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `asset_id` | uuid | NO | `—` | 关联编号（推断） |
| `asset_version_id` | uuid | NO | `—` | 关联编号（推断） |
| `file_id` | uuid | NO | `—` | 关联编号（推断） |
| `file_role` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `canvas_nodes`（画布）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `project_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `node_key` | character varying | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `node_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `title` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `position` | jsonb | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `width` | numeric | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `height` | numeric | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `metadata` | jsonb | NO | `'{}'::jsonb` | 扩展元数据 JSON；不能存权限事实或密钥 |
| `version` | bigint | NO | `1` | 乐观锁或配置版本号 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `canvas_project_assets`（画布）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `project_id` | uuid | NO | `—` | 关联编号（推断） |
| `asset_id` | uuid | NO | `—` | 关联编号（推断） |
| `asset_version_id` | uuid | NO | `—` | 关联编号（推断） |
| `added_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `canvas_projects`（画布）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `title` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `background_mode` | character varying | NO | `'lines'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `show_image_info` | boolean | NO | `false` | 业务字段（推断，需按该表业务流程补充） |
| `viewport` | jsonb | NO | `'{"k": 1, "x": 0, "y": 0}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'active'::text` | 生命周期状态；由表级约束限定枚举 |
| `version` | bigint | NO | `1` | 乐观锁或配置版本号 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |
| `deleted_at` | timestamp with time zone | YES | `—` | 软删除时间；为空表示未删除 |
| `external_key` | character varying | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |

### `collection_items`（资产与互动）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `collection_id` | uuid | NO | `—` | 关联编号（推断） |
| `asset_id` | uuid | NO | `—` | 关联编号（推断） |
| `added_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |

### `collections`（资产与互动）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `description` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `consent_records`（隐私同意）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `consent_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `policy_version` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `granted` | boolean | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `source` | character varying | NO | `'web'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `content_edit_inputs`（其他业务表）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `edit_id` | uuid | NO | `—` | 关联编号（推断） |
| `file_id` | uuid | NO | `—` | 关联编号（推断） |
| `role` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `position` | integer | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `content_edits`（其他业务表）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `daily_usage_reservations`（额度与用量）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `usage_date` | date | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `feature_code` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `amount` | numeric | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `status` | text | NO | `'reserved'::text` | 生命周期状态；由表级约束限定枚举 |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `expires_at` | timestamp with time zone | NO | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `settled_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |

### `data_deletion_requests`（其他业务表）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `status` | text | NO | `'requested'::text` | 生命周期状态；由表级约束限定枚举 |
| `reason` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `requested_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `requested_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |
| `scheduled_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `completed_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `legal_hold` | boolean | NO | `false` | 业务字段（推断，需按该表业务流程补充） |
| `result_summary` | jsonb | YES | `—` | 业务字段（推断，需按该表业务流程补充） |

### `data_export_jobs`（其他业务表）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `status` | text | NO | `'requested'::text` | 生命周期状态；由表级约束限定枚举 |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `requested_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |
| `started_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `finished_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `storage_key` | text | YES | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `checksum_sha256` | character | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `expires_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `error_message` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |

### `departments`（团队与组织）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `team_id` | uuid | NO | `—` | 所属团队编号 |
| `parent_id` | uuid | YES | `—` | 关联编号（推断） |
| `name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'active'::text` | 生命周期状态；由表级约束限定枚举 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `feedback_submissions`（反馈）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `user_id` | uuid | YES | `—` | 关联用户内部编号 |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `feedback_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `content` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `contact` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | character varying | NO | `'pending'::character varying` | 生命周期状态；由表级约束限定枚举 |
| `handled_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `handled_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `resolution` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `request_id` | character varying | YES | `—` | 关联编号（推断） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `file_jobs`（文件对象）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `file_id` | uuid | NO | `—` | 关联编号（推断） |
| `job_kind` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `status` | text | NO | `'queued'::text` | 生命周期状态；由表级约束限定枚举 |
| `attempt_count` | integer | NO | `0` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `next_run_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |
| `lease_owner` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `lease_until` | timestamp with time zone | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `lease_version` | bigint | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `last_error_code` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `upload_session_id` | uuid | YES | `—` | 关联编号（推断） |
| `generation_output_id` | uuid | YES | `—` | 关联编号（推断） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `file_objects`（文件对象）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `uploaded_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `storage_provider` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `bucket` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `object_key` | text | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `storage_version_id` | text | YES | `—` | 关联编号（推断） |
| `checksum` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `size_bytes` | bigint | YES | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `mime_type` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'pending'::text` | 生命周期状态；由表级约束限定枚举 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `deleted_at` | timestamp with time zone | YES | `—` | 软删除时间；为空表示未删除 |
| `media_type` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `source_kind` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `original_filename` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `upload_batch_id` | uuid | YES | `—` | 关联编号（推断） |
| `source_file_id` | uuid | YES | `—` | 关联编号（推断） |
| `preview_variant` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `width` | integer | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `height` | integer | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `duration_ms` | integer | YES | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `write_idempotency_key` | character varying | YES | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `retry_count` | integer | NO | `0` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `last_error_code` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |
| `inspection_status` | text | NO | `'pending'::text` | 业务字段（推断，需按该表业务流程补充） |
| `completeness` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `edit_id` | uuid | YES | `—` | 关联编号（推断） |

### `generation_attempts`（生成任务与结果）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `task_id` | uuid | NO | `—` | 关联编号（推断） |
| `attempt_no` | integer | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_task_id` | character varying | YES | `—` | 关联编号（推断） |
| `returned_output_count` | integer | YES | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `response_complete` | boolean | NO | `false` | 业务字段（推断，需按该表业务流程补充） |
| `started_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |
| `finished_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `error_code` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `generation_inputs`（生成任务与结果）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `task_id` | uuid | NO | `—` | 关联编号（推断） |
| `file_id` | uuid | NO | `—` | 关联编号（推断） |
| `role` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `position` | integer | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `generation_outputs`（生成任务与结果）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `task_id` | uuid | NO | `—` | 关联编号（推断） |
| `file_id` | uuid | YES | `—` | 关联编号（推断） |
| `thumbnail_file_id` | uuid | YES | `—` | 关联编号（推断） |
| `output_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `width` | integer | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `height` | integer | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `duration_ms` | integer | YES | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `content_status` | text | NO | `'pending'::text` | 业务字段（推断，需按该表业务流程补充） |
| `metadata` | jsonb | NO | `'{}'::jsonb` | 扩展元数据 JSON；不能存权限事实或密钥 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `attempt_id` | uuid | YES | `—` | 关联编号（推断） |
| `output_index` | integer | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_output_id` | character varying | YES | `—` | 关联编号（推断） |
| `availability` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `recovery_source_ciphertext` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |

### `generation_tasks`（生成任务与结果）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `task_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `model` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `model_version` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `pricing_version` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `prompt` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `parameters` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'queued'::text` | 生命周期状态；由表级约束限定枚举 |
| `request_id` | character varying | NO | `—` | 关联编号（推断） |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `error_code` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `started_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `finished_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |
| `quota_reservation_id` | uuid | YES | `—` | 关联编号（推断） |
| `daily_reservation_id` | uuid | YES | `—` | 关联编号（推断） |
| `usage_record_id` | uuid | YES | `—` | 关联编号（推断） |
| `timeout_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `provider_task_id` | character varying | YES | `—` | 关联编号（推断） |
| `error_message` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `outputs` | jsonb | NO | `'[]'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `requested_output_count` | integer | YES | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |

### `invoice_request_orders`（订阅与计费）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `request_id` | uuid | NO | `—` | 关联编号（推断） |
| `order_id` | uuid | NO | `—` | 关联编号（推断） |
| `order_no` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `amount_minor` | bigint | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |

### `invoice_requests`（订阅与计费）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `title_type` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `title_name` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `tax_no` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `email` | text | NO | `—` | 邮箱或联系地址 |
| `total_minor` | bigint | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'pending'::text` | 生命周期状态；由表级约束限定枚举 |
| `pdf_url` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `reject_reason` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |
| `completed_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |

### `job_titles`（团队与组织）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `team_id` | uuid | NO | `—` | 所属团队编号 |
| `name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'active'::text` | 生命周期状态；由表级约束限定枚举 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `membership_events`（团队与组织）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `membership_id` | uuid | NO | `—` | 关联编号（推断） |
| `event_type` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `actor_user_id` | uuid | YES | `—` | 关联编号（推断） |
| `reason` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `request_id` | character varying | YES | `—` | 关联编号（推断） |
| `occurred_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |
| `metadata` | jsonb | NO | `'{}'::jsonb` | 扩展元数据 JSON；不能存权限事实或密钥 |

### `model_catalog`（模型与渠道）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | bigint | NO | `—` | 主键编号 |
| `model_id` | character varying | YES | `—` | 关联编号（推断） |
| `display_name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider` | character varying | NO | `'unknown'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `capability` | character varying | NO | `'text'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `visible` | boolean | NO | `true` | 业务字段（推断，需按该表业务流程补充） |
| `sort_order` | integer | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `model_catalog_channel_models`（模型与渠道）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | bigint | NO | `—` | 主键编号 |
| `model_catalog_id` | bigint | NO | `—` | 关联编号（推断） |
| `platform_api_key_id` | uuid | NO | `—` | 关联编号（推断） |
| `upstream_model_id` | character varying | NO | `—` | 关联编号（推断） |
| `priority` | integer | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `is_active` | boolean | NO | `true` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `moderation_records`（内容安全）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `asset_id` | uuid | YES | `—` | 关联编号（推断） |
| `comment_id` | uuid | YES | `—` | 关联编号（推断） |
| `decision` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `reason` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `reviewer_user_id` | uuid | YES | `—` | 关联编号（推断） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |

### `notifications`（通知）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `title` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `body` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `payload` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `read_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `orders`（订阅与计费）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `plan_id` | uuid | YES | `—` | 关联编号（推断） |
| `order_no` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'pending'::text` | 生命周期状态；由表级约束限定枚举 |
| `currency` | character varying | NO | `'CNY'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `amount_minor` | bigint | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `expires_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |
| `plan_version_id` | uuid | YES | `—` | 关联编号（推断） |
| `price_snapshot` | jsonb | NO | `'{}'::jsonb` | 数量、金额或度量字段（推断，单位以接口约定为准） |

### `outbox_events`（同步事件）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `event_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `schema_version` | integer | NO | `1` | 业务字段（推断，需按该表业务流程补充） |
| `aggregate_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `aggregate_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `payload` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `status` | character varying | NO | `'pending'::character varying` | 生命周期状态；由表级约束限定枚举 |
| `attempts` | integer | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `available_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |
| `published_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `sequence_no` | bigint | NO | `—` | 业务字段（推断，需按该表业务流程补充） |

### `payment_customers`（订阅与计费）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `provider` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_customer_id` | character varying | NO | `—` | 关联编号（推断） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `payment_events`（订阅与计费）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `provider` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_event_id` | character varying | NO | `—` | 关联编号（推断） |
| `event_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `order_id` | uuid | YES | `—` | 关联编号（推断） |
| `payment_id` | uuid | YES | `—` | 关联编号（推断） |
| `amount_minor` | bigint | YES | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `currency` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'received'::text` | 生命周期状态；由表级约束限定枚举 |
| `payload` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `signature_verified` | boolean | NO | `false` | 业务字段（推断，需按该表业务流程补充） |
| `error_message` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `received_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |
| `processed_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `attempt_count` | integer | NO | `0` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `next_retry_at` | timestamp with time zone | YES | `now()` | 时间字段（UTC，具体含义按表内状态解释） |
| `last_attempt_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |

### `payments`（订阅与计费）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `order_id` | uuid | NO | `—` | 关联编号（推断） |
| `provider` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_payment_id` | character varying | YES | `—` | 关联编号（推断） |
| `status` | text | NO | `'pending'::text` | 生命周期状态；由表级约束限定枚举 |
| `amount_minor` | bigint | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `raw_reference` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `paid_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `currency` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |

### `permission_overrides`（权限）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `permission_code` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `effect` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `reason` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `expires_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `permissions`（权限）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `code` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `description` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `plan_features`（订阅与计费）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `plan_id` | uuid | NO | `—` | 关联编号（推断） |
| `feature_code` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `enabled` | boolean | NO | `true` | 业务字段（推断，需按该表业务流程补充） |

### `plan_quotas`（订阅与计费）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `plan_id` | uuid | NO | `—` | 关联编号（推断） |
| `quota_code` | character varying | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `amount` | numeric | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `unit` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `reset_interval` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |

### `plan_versions`（订阅与计费）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `plan_id` | uuid | NO | `—` | 关联编号（推断） |
| `version` | integer | NO | `—` | 乐观锁或配置版本号 |
| `currency` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `price_minor` | bigint | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `billing_interval` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `feature_snapshot` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `quota_snapshot` | jsonb | NO | `'{}'::jsonb` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `effective_at` | timestamp with time zone | NO | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `retired_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `plans`（订阅与计费）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `code` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `description` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `currency` | character varying | NO | `'CNY'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `price_minor` | bigint | NO | `0` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `billing_interval` | text | NO | `'month'::text` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'active'::text` | 生命周期状态；由表级约束限定枚举 |
| `version` | integer | NO | `1` | 乐观锁或配置版本号 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `platform_alerts`（平台运维）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `alert_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `severity` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `summary` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `detail` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `status` | character varying | NO | `'open'::character varying` | 生命周期状态；由表级约束限定枚举 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `acknowledged_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `acknowledged_by` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |

### `platform_api_keys`（平台运维）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider` | character varying | NO | `'openai'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `base_url` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `api_key` | text | YES | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `model` | character varying | NO | `''::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `max_concurrency` | integer | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `is_active` | boolean | NO | `true` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |
| `api_key_ciphertext` | text | YES | `—` | 敏感凭据或其引用；禁止明文输出（推断） |

### `plugin_versions`（插件）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `plugin_id` | uuid | NO | `—` | 关联编号（推断） |
| `version` | character varying | NO | `—` | 乐观锁或配置版本号 |
| `manifest` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `package_hash` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `released_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `plugins`（插件）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `plugin_key` | character varying | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `display_name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `source_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `source_uri` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'active'::text` | 生命周期状态；由表级约束限定枚举 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `prompt_sources`（提示词）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `source_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `source_uri` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'active'::text` | 生命周期状态；由表级约束限定枚举 |
| `refresh_interval_seconds` | integer | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `prompt_tags`（提示词）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |

### `prompt_template_tags`（提示词）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `template_id` | uuid | NO | `—` | 关联编号（推断） |
| `tag_id` | uuid | NO | `—` | 关联编号（推断） |

### `prompt_template_versions`（提示词）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `template_id` | uuid | NO | `—` | 关联编号（推断） |
| `version` | integer | NO | `—` | 乐观锁或配置版本号 |
| `body` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `variables` | jsonb | NO | `'[]'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `content_hash` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `prompt_templates`（提示词）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `source_id` | uuid | YES | `—` | 关联编号（推断） |
| `template_key` | character varying | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `title` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'active'::text` | 生命周期状态；由表级约束限定枚举 |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `provider_call_daily`（模型与渠道）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `day` | date | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_id` | uuid | YES | `—` | 关联编号（推断） |
| `model` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `capability` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `request_count` | bigint | NO | `0` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `success_count` | bigint | NO | `0` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `failure_count` | bigint | NO | `0` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `input_units` | numeric | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `output_units` | numeric | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `total_cost_minor` | bigint | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `provider_credentials`（模型与渠道）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `provider_id` | uuid | NO | `—` | 关联编号（推断） |
| `name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `secret_ref` | text | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `key_version` | character varying | YES | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `status` | text | NO | `'active'::text` | 生命周期状态；由表级约束限定枚举 |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `quota_accounts`（额度与用量）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `quota_code` | character varying | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `granted` | numeric | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `reserved` | numeric | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `consumed` | numeric | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `available` | numeric | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `version` | bigint | NO | `1` | 乐观锁或配置版本号 |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `quota_allocations`（额度与用量）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `grant_id` | uuid | NO | `—` | 关联编号（推断） |
| `usage_record_id` | uuid | YES | `—` | 关联编号（推断） |
| `amount` | numeric | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `status` | text | NO | `'reserved'::text` | 生命周期状态；由表级约束限定枚举 |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `settled_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |

### `quota_grants`（额度与用量）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `quota_code` | character varying | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `source_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `source_id` | uuid | YES | `—` | 关联编号（推断） |
| `granted` | numeric | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `reserved` | numeric | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `consumed` | numeric | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `expires_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `quota_ledger`（额度与用量）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `account_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `reservation_id` | uuid | YES | `—` | 关联编号（推断） |
| `entry_type` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `amount` | numeric | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `metadata` | jsonb | NO | `'{}'::jsonb` | 扩展元数据 JSON；不能存权限事实或密钥 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `quota_reservations`（额度与用量）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `account_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `quota_code` | character varying | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `amount` | numeric | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `status` | text | NO | `'reserved'::text` | 生命周期状态；由表级约束限定枚举 |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `expires_at` | timestamp with time zone | NO | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `settled_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |

### `redeem_codes`（其他业务表）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `code` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `kind` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `denomination` | numeric | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `plan_id` | uuid | YES | `—` | 关联编号（推断） |
| `batch` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `expires_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `used_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `used_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `refunds`（订阅与计费）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `order_id` | uuid | NO | `—` | 关联编号（推断） |
| `payment_id` | uuid | YES | `—` | 关联编号（推断） |
| `amount_minor` | bigint | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `currency` | character varying | NO | `'CNY'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'pending'::text` | 生命周期状态；由表级约束限定枚举 |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `provider_refund_id` | character varying | YES | `—` | 关联编号（推断） |
| `reason` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `requested_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `raw_reference` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |
| `completed_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `refund_kind` | text | NO | `'purchase_refund'::text` | 业务字段（推断，需按该表业务流程补充） |

### `renewal_attempts`（其他业务表）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `cycle_id` | uuid | NO | `—` | 关联编号（推断） |
| `attempt_no` | integer | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_account_id` | character varying | NO | `—` | 关联编号（推断） |
| `environment` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_request_id` | character varying | YES | `—` | 关联编号（推断） |
| `provider_payment_id` | character varying | YES | `—` | 关联编号（推断） |
| `status` | text | NO | `'queued'::text` | 生命周期状态；由表级约束限定枚举 |
| `failure_class` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `scheduled_at` | timestamp with time zone | NO | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `sent_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `next_retry_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `completed_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `renewal_mandates`（其他业务表）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `subscription_id` | uuid | NO | `—` | 关联编号（推断） |
| `provider` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_account_id` | character varying | NO | `—` | 关联编号（推断） |
| `environment` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `collection_owner` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_mandate_id` | character varying | YES | `—` | 关联编号（推断） |
| `authorized_by` | uuid | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `terms_version` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `terms_hash` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `consented_at` | timestamp with time zone | NO | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `plan_id` | uuid | NO | `—` | 关联编号（推断） |
| `plan_version_id` | uuid | NO | `—` | 关联编号（推断） |
| `max_amount_minor` | bigint | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `currency` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `billing_interval` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `billing_timezone` | character varying | NO | `'UTC'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'pending'::text` | 生命周期状态；由表级约束限定枚举 |
| `authorized_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `cancel_requested_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `cancelled_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `provider_status` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `renewal_reminders`（其他业务表）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `cycle_id` | uuid | NO | `—` | 关联编号（推断） |
| `reminder_type` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `channel` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `destination_ref` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `scheduled_at` | timestamp with time zone | NO | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `sent_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `delivery_status` | text | NO | `'queued'::text` | 业务字段（推断，需按该表业务流程补充） |
| `template_version` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `content_hash` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `resource_tombstones`（生命周期）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `resource_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `resource_id` | uuid | NO | `—` | 关联编号（推断） |
| `deleted_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `reason` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `payload_hash` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `deleted_at` | timestamp with time zone | NO | `now()` | 软删除时间；为空表示未删除 |
| `purge_after` | timestamp with time zone | YES | `—` | 业务字段（推断，需按该表业务流程补充） |

### `restore_drills`（备份与恢复）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `backup_id` | uuid | NO | `—` | 关联编号（推断） |
| `environment` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | text | NO | `'started'::text` | 生命周期状态；由表级约束限定枚举 |
| `target_time` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `restored_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `completed_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `rpo_seconds` | bigint | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `rto_seconds` | bigint | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `checks` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `difference_summary` | jsonb | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `error_message` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `role_bindings`（权限）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `role_id` | uuid | NO | `—` | 关联编号（推断） |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `role_permissions`（权限）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `role_id` | uuid | NO | `—` | 关联编号（推断） |
| `permission_id` | uuid | NO | `—` | 关联编号（推断） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `roles`（权限）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `code` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `description` | text | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `schema_migrations`（平台运维）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `version` | text | NO | `—` | 乐观锁或配置版本号 |
| `checksum` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `applied_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |

### `security_events`（审计与安全）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `user_id` | uuid | YES | `—` | 关联用户内部编号 |
| `session_id` | uuid | YES | `—` | 关联编号（推断） |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `event_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `result` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `request_id` | character varying | YES | `—` | 关联编号（推断） |
| `ip_hash` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `user_agent_hash` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `details` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `occurred_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |

### `session_actions`（其他业务表）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `actor_user_id` | uuid | NO | `—` | 关联编号（推断） |
| `target_user_id` | uuid | NO | `—` | 关联编号（推断） |
| `target_session_id` | uuid | YES | `—` | 关联编号（推断） |
| `action_type` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `reason` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `request_id` | character varying | YES | `—` | 关联编号（推断） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `subscription_events`（订阅与计费）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `subscription_id` | uuid | NO | `—` | 关联编号（推断） |
| `event_type` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `provider_event_id` | character varying | YES | `—` | 关联编号（推断） |
| `payload` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `occurred_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `subscriptions`（订阅与计费）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `plan_id` | uuid | NO | `—` | 关联编号（推断） |
| `status` | text | NO | `'trialing'::text` | 生命周期状态；由表级约束限定枚举 |
| `provider` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_subscription_id` | character varying | YES | `—` | 关联编号（推断） |
| `current_period_start` | timestamp with time zone | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `current_period_end` | timestamp with time zone | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `cancel_at_period_end` | boolean | NO | `false` | 业务字段（推断，需按该表业务流程补充） |
| `version` | bigint | NO | `1` | 乐观锁或配置版本号 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |
| `plan_version_id` | uuid | YES | `—` | 关联编号（推断） |
| `billing_anchor_month` | smallint | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `billing_anchor_day` | smallint | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `billing_timezone` | character varying | NO | `'UTC'::character varying` | 业务字段（推断，需按该表业务流程补充） |

### `sync_cursors`（同步事件）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `device_id` | uuid | NO | `—` | 关联编号（推断） |
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `last_sequence` | bigint | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `version` | bigint | NO | `1` | 乐观锁或配置版本号 |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `system_settings`（平台运维）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `setting_key` | character varying | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `setting_value` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `team_invitations`（团队与组织）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `team_id` | uuid | NO | `—` | 所属团队编号 |
| `email` | text | NO | `—` | 邮箱或联系地址 |
| `invited_user_id` | uuid | YES | `—` | 关联编号（推断） |
| `invited_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `token_hash` | text | NO | `—` | 敏感凭据或其引用；禁止明文输出（推断） |
| `status` | text | NO | `'pending'::text` | 生命周期状态；由表级约束限定枚举 |
| `expires_at` | timestamp with time zone | NO | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `accepted_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `team_memberships`（团队与组织）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `team_id` | uuid | NO | `—` | 所属团队编号 |
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `department_id` | uuid | YES | `—` | 关联编号（推断） |
| `job_title_id` | uuid | YES | `—` | 关联编号（推断） |
| `status` | USER-DEFINED (membership_status) | NO | `'invited'::app.membership_status` | 生命周期状态；由表级约束限定枚举 |
| `joined_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `left_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `invited_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `teams`（团队与组织）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `slug` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `owner_user_id` | uuid | NO | `—` | 关联编号（推断） |
| `status` | USER-DEFINED (team_status) | NO | `'active'::app.team_status` | 生命周期状态；由表级约束限定枚举 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `upload_batches`（其他业务表）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `created_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `upload_session_parts`（其他业务表）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `session_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `part_number` | integer | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `etag` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `size_bytes` | bigint | NO | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `checksum` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `upload_sessions`（其他业务表）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `file_id` | uuid | NO | `—` | 关联编号（推断） |
| `attempt_no` | integer | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_upload_id` | text | YES | `—` | 关联编号（推断） |
| `status` | text | NO | `'initiated'::text` | 生命周期状态；由表级约束限定枚举 |
| `expires_at` | timestamp with time zone | NO | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `last_error_code` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `usage_reconciliations`（额度与用量）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `usage_record_id` | uuid | NO | `—` | 关联编号（推断） |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `actor_admin` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `decision` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `reason` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider_reference` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |

### `usage_records`（额度与用量）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `user_id` | uuid | YES | `—` | 关联用户内部编号 |
| `feature_code` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `provider` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `model` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `quantity` | numeric | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `unit` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `result` | text | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `idempotency_key` | character varying | NO | `—` | 幂等编号；重复请求必须复用同一结果 |
| `request_id` | character varying | YES | `—` | 关联编号（推断） |
| `occurred_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |
| `metadata` | jsonb | NO | `'{}'::jsonb` | 扩展元数据 JSON；不能存权限事实或密钥 |
| `quota_reservation_id` | uuid | YES | `—` | 关联编号（推断） |
| `daily_reservation_id` | uuid | YES | `—` | 关联编号（推断） |
| `completed_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `latency_ms` | integer | YES | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |

### `user_accounts`（用户与安全）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `appwrite_user_id` | character varying | NO | `—` | 关联编号（推断） |
| `email` | text | YES | `—` | 邮箱或联系地址 |
| `display_name` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `avatar_file_id` | uuid | YES | `—` | 关联编号（推断） |
| `locale` | character varying | NO | `'zh-CN'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `timezone` | character varying | NO | `'Asia/Shanghai'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `status` | USER-DEFINED (user_status) | NO | `'active'::app.user_status` | 生命周期状态；由表级约束限定枚举 |
| `auth_version` | bigint | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `last_login_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |
| `max_active_sessions` | integer | NO | `3` | 业务字段（推断，需按该表业务流程补充） |
| `invited_by_user_id` | uuid | YES | `—` | 关联编号（推断） |

### `user_devices`（用户与安全）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `installation_id` | uuid | NO | `—` | 关联编号（推断） |
| `display_name` | character varying | NO | `'未命名设备'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `client_type` | character varying | NO | `'web'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `os_family` | character varying | NO | `'unknown'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `browser_family` | character varying | NO | `'unknown'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `last_seen_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `archived_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |

### `user_preferences`（用户与安全）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `values` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `version` | bigint | NO | `1` | 乐观锁或配置版本号 |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `user_sessions`（用户与安全）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `user_id` | uuid | NO | `—` | 关联用户内部编号 |
| `device_id` | uuid | NO | `—` | 关联编号（推断） |
| `identity_provider` | character varying | NO | `'appwrite'::character varying` | 业务字段（推断，需按该表业务流程补充） |
| `provider_session_id` | character varying | NO | `—` | 关联编号（推断） |
| `admission_status` | USER-DEFINED (session_status) | NO | `'pending'::app.session_status` | 业务字段（推断，需按该表业务流程补充） |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `expires_at` | timestamp with time zone | NO | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `last_seen_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `revoked_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `revoked_reason` | character varying | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `admitted_auth_version` | bigint | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `provider_revocation_status` | USER-DEFINED (provider_revoke_status) | NO | `'not_requested'::app.provider_revoke_status` | 业务字段（推断，需按该表业务流程补充） |
| `revoke_retry_at` | timestamp with time zone | YES | `—` | 时间字段（UTC，具体含义按表内状态解释） |
| `revoke_attempts` | integer | NO | `0` | 业务字段（推断，需按该表业务流程补充） |
| `is_online` | boolean | NO | `false` | 业务字段（推断，需按该表业务流程补充） |

### `v_quota_reconciliation`（派生视图）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `quota_account_id` | uuid | YES | `—` | 关联编号（推断） |
| `workspace_id` | uuid | YES | `—` | 所属工作空间；多租户隔离的核心字段 |
| `quota_code` | character varying | YES | `—` | 数量、金额或度量字段（推断，单位以接口约定为准） |
| `granted` | numeric | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `reserved` | numeric | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `consumed` | numeric | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `available` | numeric | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `ledger_reserved` | numeric | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `ledger_committed` | numeric | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `ledger_released` | numeric | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `drift` | numeric | YES | `—` | 业务字段（推断，需按该表业务流程补充） |

### `workspace_plugins`（空间与多租户）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `workspace_id` | uuid | NO | `—` | 所属工作空间；多租户隔离的核心字段 |
| `plugin_id` | uuid | NO | `—` | 关联编号（推断） |
| `plugin_version_id` | uuid | YES | `—` | 关联编号（推断） |
| `status` | text | NO | `'enabled'::text` | 生命周期状态；由表级约束限定枚举 |
| `config` | jsonb | NO | `'{}'::jsonb` | 业务字段（推断，需按该表业务流程补充） |
| `installed_by` | uuid | YES | `—` | 业务字段（推断，需按该表业务流程补充） |
| `installed_at` | timestamp with time zone | NO | `now()` | 时间字段（UTC，具体含义按表内状态解释） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

### `workspaces`（空间与多租户）

| 字段 | 类型 | 可空 | 默认值 | 用途 |
|---|---|---|---|---|
| `id` | uuid | NO | `gen_random_uuid()` | 主键编号 |
| `type` | USER-DEFINED (workspace_type) | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `team_id` | uuid | YES | `—` | 所属团队编号 |
| `owner_user_id` | uuid | YES | `—` | 关联编号（推断） |
| `name` | character varying | NO | `—` | 业务字段（推断，需按该表业务流程补充） |
| `status` | USER-DEFINED (workspace_status) | NO | `'active'::app.workspace_status` | 生命周期状态；由表级约束限定枚举 |
| `version` | bigint | NO | `1` | 乐观锁或配置版本号 |
| `created_at` | timestamp with time zone | NO | `now()` | 创建时间（UTC） |
| `updated_at` | timestamp with time zone | NO | `now()` | 最后更新时间（UTC） |

## 维护规则

1. 新增业务数据先确定归属空间、所有者、生命周期、审计要求和删除策略，再加字段。
2. 金额使用最小货币单位整数；时间使用 UTC `timestamptz`；跨请求写入必须有幂等编号。
3. 密码、长期 Token、供应商密钥不进入普通业务字段；密钥只能存服务端加密密文或引用。
4. 任何迁移必须更新 `MANIFEST.sha256.json`，并通过空库重放、数据库测试和恢复演练。
5. 本文由项目迁移重建生成；如果线上数据库与项目迁移冲突，先核实差异再更新迁移或本文。
